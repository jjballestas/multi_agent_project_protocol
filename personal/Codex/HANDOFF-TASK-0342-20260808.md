# HANDOFF TASK-0342

Owner/maker: Codex
Reviewer/checker: independent peer required
Status requested: in_review

## Delivered

- Implementation commit: `7bbc02538c53abf41fa403994fde023358ce28a2`.
- Memory commit: `670e3879bbf3dda2662cf088999367d510ce3184`.
- `scripts/scan_encoding.ps1` now constructs the excluded-directory boundary with the
  host `DirectorySeparatorChar` after trimming both host separator variants. No literal
  Windows slash participates in the exclusion decision.
- `examples/encoding_gate_cases/run_encoding_gate_cases.py` compares Python and PowerShell
  behavior on the same tree. Both exclude `runtime/memory/index.db` and report exactly the
  same visible finding.
- Permanent negative `NEG-ENCODING-SKIP-PATH-SEPARATOR` restores the literal-backslash bug.
  On POSIX it exposes `runtime/memory/index.db` and fails the contract.
- Test scratch is outside the attested tree under the per-project Aegis scratch umbrella.

## Evidence

- GitHub Actions run: `31266732042`.
- Job `validate`, step `Scan encoding with PowerShell`: success, 2026-08-08T16:24:09Z to
  2026-08-08T16:24:20Z.
- Same job, step `Run encoding gate cases`: success; output confirms Python cases,
  PowerShell parity, and separator mutation.
- CI falsification inventory: 62/62 contracts across 10/10 wired runners.
- Local collaboration, Python encoding, Windows-PowerShell encoding, neutrality,
  falsification, compile, drift, and diff gates exited 0.
- Local host lacks PowerShell 7, so POSIX behavior was deliberately left unclaimed locally
  and measured by the required ubuntu-latest run.

## Boundaries

- This task does not absorb TASK-0338 or TASK-0336. The shared root is the same: binding a
  platform/language representation instead of the executed property.
- Run `31266732042` later failed at `Run mailbox status validation cases`; that masked defect
  is separate. The known `falsification-runners` retry failure is also separate.
- Codex implemented the change and has not reviewed or ratified it.
