# TASK-0414 remediation r2 memory

- Implementation commit `e5d79eeb` resolves event-auth HMAC material by the `event_auth.key_id`
  declared in each event, instead of selecting the current key by actor.
- Only an independently verifiable `event_auth.key_rotation_declared` event may authorize an
  absent historical key id as `key_unavailable`; an undeclared unknown id remains fatal.
- The structured drift result carries the absolute boundary inventory without altering canonical
  state. The 1,009-event fixture reports 1,009 unavailable, zero invalid; counterproof exits are
  declared-v1 `0`, undeclared-unknown `1`, and bad-present `1`.
- The superseded actor-attestation change from `be3edb87` is reverted. `actor_auth` does not share
  the actor-resolution defect: it validates and indexes by its declared keyid.
- Focused replay and actor-signature suites, falsification inventory 76/76, collaboration,
  encoding, and both neutrality scanners exited 0. Independent review remains required.
- Governed delivery commit `0c2df5fc` moves TASK-0414 to `in_review`, releases every Codex claim,
  and publishes the self-contained Arquitecto handoff. Codex remains maker only.
