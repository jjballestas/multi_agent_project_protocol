# TASK-0367 implementation memory

- Implementation commit `503303c9` removes inherited concrete participant identities from the
  generated neutral core while preserving the live instance's configured roles.
- Generic absent-config defaults are `architect`, `implementer`, and `human_owner`; routing and
  prune actor fallbacks use `architect`. The prune sentinel refers to project memory.
- The peer mailbox harness derives its executable command from the configured `PeerId` instead of
  embedding a participant name.
- The runtime-instantiation runner now proves configured roles load and route operationally, and
  mutation-proves that injecting the generated instance's configured architect identity into core
  code makes its neutrality scanner exit nonzero.
- Before implementation, the runner exited 1 on the coordination and runtime cases with four and
  five derived identity findings. After implementation it exits 0 for all 10 cases; the expected
  placeholder-negative diagnostic is part of the passing case.
- Exact implementation commit `503303c9` passed the runtime-instantiation runner, encoding,
  neutrality, collaboration, compile, diff, and empty tracked-status gates in detached clean
  worktree `D:/Aegis_Scratch/multi_agent_project_protocol/task0367-clean-503303c9`. Governed
  delivery commit `1be21153` moves TASK-0367 to `in_review`, releases both Codex claims, and
  publishes the self-contained Arquitecto handoff. Codex is maker only and did not review or
  ratify the implementation.
