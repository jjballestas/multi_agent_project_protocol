# TASK-0410 remediation r2

- Implementation commit `b899167b` changes the PowerShell configured-identity set to
  `StringComparer::Ordinal` and adds a production-mutation guard for ordinal digest matching.
- The exact digest regression (`-contains`) makes the new focused test exit 1; restored production
  passes it.
- `python -m unittest scripts.test_scan_domain_neutrality` passed twice: 11 tests in 458.536s and
  459.459s. Falsification inventory 77/77, both neutrality scanners, encoding, and collaboration
  validation exited 0.
- RES-1 line splitting is intentionally untouched and remains routed to TASK-0338.
- TASK-0410 remains `in_progress` under the r2 maker claim pending governed delivery to independent
  review. Codex has not reviewed or ratified the work.
