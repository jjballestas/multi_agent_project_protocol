# TASK-0410 remediation r2

- Implementation commit `b899167b` changes the PowerShell configured-identity set to
  `StringComparer::Ordinal` and adds a production-mutation guard for ordinal digest matching.
- The exact digest regression (`-contains`) makes the new focused test exit 1; restored production
  passes it.
- `python -m unittest scripts.test_scan_domain_neutrality` passed twice: 11 tests in 458.536s and
  459.459s. Falsification inventory 77/77, both neutrality scanners, encoding, and collaboration
  validation exited 0.
- RES-1 line splitting is intentionally untouched and remains routed to TASK-0338.
- TASK-0410 remains `in_progress` under the r2 maker claim pending governed delivery to independent
  review. Codex has not reviewed or ratified the work.

## Governed delivery

- Delivery commit `872f5baa` moves TASK-0410 to `in_review`, releases both Codex r2 claims, and
  publishes `MSG-20260822-Codex-to-Arquitecto-HANDOFF-TASK-0410-r2.md`.
- Runtime materialization reached seq 10117 with drift false. The delivery gates exited 0 for both
  neutrality scanners, encoding, and collaboration validation.
- Independent Analista re-review is required; Codex remains maker only.

## Final closure

- Codex-signed runtime events 10151-10153 acquired and released the closing claim and moved
  TASK-0410 from `review_approved` to `done`, anchored at implementation commit `b899167b`.
- Two residuals remain routed to TASK-0338: RES-1 is the coordinate-axis line-splitting defect;
  RES-2-GUARD is the unguarded ordinal repair at line 211, recorded there as AC7.
- This closure accredits the box behavior repaired by TASK-0410. It does not accredit identity
  inventory parity because the coordinate residual remains open.
