# HANDOFF TASK-0343

Maker: Codex
Implementation commit: `26b33967`
Delivery base: `b2da30ce`

## Result

The rollback fixture now captures the complete signed event list and claims state
immediately before rollback and compares both with the final governed state. It no
longer requires literal sequence counters or `proof=disk`, and production rollback
code is unchanged.

## Pre-fix diagnosis

- Local Windows: `seq_before=0`, `seq_after=3`, `proof=disk`, produced by the stable
  disk-proof branch in `Restore-TransientExecResidue`.
- Actions run 31269392388: no preserved record, hence none of the three fields were
  emitted.
- Actions run 31269815427 attempt 2: `head_changed`, quarantine success,
  `rollback_probe_failed`, then `ledger_unreadable_after_exec`. The trap and
  unreadable-head guard produced those conservative defers while the signed ledger
  survived unchanged.
- Full written diagnosis and the whole-file literal inventory are in
  `personal/Codex/DIAG-TASK-0343-before-fix-20260808.md`.

## Falsification and inventory

- New permanent contract: `retry-ledger-preservation-property`.
- It kills an accept-loss mutant and the former literal-log-path mutant, including
  preservation through an alternate defer path.
- Inventory: 64/64 permanent negatives declared.
- No host-absolute path assertion remains in the runner. Remaining numeric literals
  are explicit state-machine, timing, liveness, or mutation cardinalities; the two
  adjacent literal ledger sequence checks were removed with the log literal.

## Evidence

- Real Actions run 31270228630: `Execute mailbox retry falsification runner` success.
- Exact commit `26b33967` passed in clean clone
  `D:/Aegis_Scratch/multi_agent_project_protocol/codex0343-26b33967` with empty status:
  complete mailbox retry runner, 64/64 contract inventory, collaboration validator,
  encoding scan, neutrality scan, and `git diff --check`.
- The same gates passed in the live tree before commit.
- The overall Actions workflow remains red only in the separately owned neutrality
  validation step; TASK-0343's required Windows step is green.

Independent review must recompute commit `26b33967`. Codex has not reviewed or
ratified its own work.
