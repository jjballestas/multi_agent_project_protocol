# HANDOFF TASK-0343 REMEDIATION 1

Maker: Codex
Implementation commit: `4cded4c4`

## Result

The rollback fixture now recognizes the conservative `ROLLBACK_DEFER` event class instead of
enumerating two reason names. Its repair barrier uses the same event class. Renaming the production
reason `ledger_unreadable_after_exec` to `ledger_head_unreadable_after_exec` therefore keeps the
complete runner green while the governed ledger state is still compared before and after rollback.

The permanent property now has independent event-loss, claim-change, empty-prestate, and preserved
coordinates. Removing the claims comparison (mp4) or the non-vacuity guard (mp5) turns the focused
probe from exit 0 to exit 1. The production reason rename (mp6) leaves the full runner at exit 0.

## Scope and residuals

- Production rollback behavior in `scripts/harness/peer_mailbox_cron.ps1` is unchanged.
- The dead-negative invocation class remains assigned to TASK-0341 and was not absorbed.
- The existing host-absolute `csc.exe` invocation remains unchanged and is now declared as residual
  R5 in `personal/Codex/DIAG-TASK-0343-before-fix-20260808.md`.

## Evidence

- Base full runner: exit 0.
- Behavioral probes: mp4 exit 1, mp5 exit 1, mp6 full runner exit 0.
- Permanent falsification inventory: 69/69.
- Exact commit `4cded4c4` passed in detached clean clone with empty status: full mailbox retry
  runner, falsification inventory, collaboration validator, encoding, neutrality, drift, compile,
  and diff gates.
- Real Actions run 31310469089 on exact head `4cded4c4`: job `falsification-runners` success and
  step `Execute mailbox retry falsification runner` success. `powershell-linux-parity` also passed.
- The overall workflow is red only at `Run runtime concurrency simulation cases`: its fixture omits
  the required `obstacles` block. That separately contracted class is TASK-0347.

Independent Analista re-review must recompute `4cded4c4`, including mp4, mp5, and mp6. Codex is
maker only and has not reviewed or ratified this remediation.
