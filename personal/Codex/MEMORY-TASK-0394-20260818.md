# TASK-0394 implementation memory

- Maker commit `fe660a25` derives Python and PowerShell default globs from the same declared
  criterion: explicit masters plus recursive `scripts/`, `skills/`, `.githooks/`, and `runtime/`.
- Effective configured globs are checked against that criterion; an exportable file placed in an
  uncovered nested directory makes both implementations exit 1 instead of silently disappearing.
- The report names the criterion and the excluded live/instance-specific route families.
- Both implementations include `scripts/harness/peer_mailbox_cron.ps1` and
  `skills/session-watchdogs.skill.md`; Python/PowerShell reports are identical.
- `protocol.config.json` was not changed. Collaboration, encoding, and Python neutrality gates
  exited 0 before the implementation commit. Codex remains maker; independent review is required.
