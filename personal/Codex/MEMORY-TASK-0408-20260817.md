# TASK-0408 implementation checkpoint

- Commit `0b942c09` persists terminal `retry_exhausted` alerts and overdue board obligations in
  `.protocol-tmp/<peer>_mailbox_cron/<peer>_mailbox_cron.alerts.json`.
- The minimum discriminator is a durable unacknowledged obligation, not cron liveness: terminal
  retry evidence, or `in_progress`/`in_review` responsibility with no active claim, no matching
  live exec, and age beyond `StalledTaskMinutes` (default 30).
- The focused property proves fresh-window silence, overdue alerting, and rejection of a mutation
  that removes the terminal retry alert.
- Encoding, Python and PowerShell neutrality, and collaboration validation exited 0. The broad
  exec-lease suite had 29/32 pass; three established timing/liveness cases failed outside the new
  focused contract.
- TASK-0408 remains maker-owned and `in_progress` pending governed delivery and independent review.
