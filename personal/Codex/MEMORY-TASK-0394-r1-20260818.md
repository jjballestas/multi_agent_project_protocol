# TASK-0394 remediation r1

- Maker commit `50532ee7` replaces the vacuous default-list self-comparison with a tree census of
  generic `.py`, `.ps1`, and `.skill.md` payload candidates outside declared instance-only roots.
- A new `tools/exportable.py` root now makes both upgrade twins exit 1; the baseline remains green
  and Python/PowerShell reports are line-identical.
- Default coordination instancing now ships `scripts/harness/` with the watchdog skill, including
  `test_session_watchdog_filter.py`.
- `scripts/test_upgrade_instance_contract.py` permanently proves the tree-negative and guide/proof
  co-delivery. The focused contract, real coordination scaffold, encoding, neutrality, and canonical
  collaboration gates exited 0.
- TASK-0394 remains `in_progress`. Independent checker re-judgment is required before the governed
  delivery commit; Codex has not reviewed or ratified the remediation.
