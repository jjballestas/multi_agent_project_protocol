# TASK-0408 remediation r1

- Implementation commit `d8a7ceb7` makes `stalled_task` suppression require a non-released claim
  with a readable future `expires_at`; missing, malformed, and expired values alert.
- `EXEC_EXIT code=-1 outcome=transient` now exhausts immediately and persists `retry_exhausted`.
- The focused property ran twice and covers no claim, current claim, expired claim, the old
  status-only mutant, and the retry-count-only `-1` mutant.
- Collaboration, encoding, and Python neutrality gates each exited 0 twice before commit.
- The broad 32-case exec-lease suite passed the TASK-0408 property but retained three unrelated
  liveness failures already present in this worktree lineage. PowerShell neutrality timed out;
  Python neutrality passed and both scanner exemption maps were advanced together.
- TASK-0408 remains `in_progress` pending the checker-requested A/B re-judgment before governed
  delivery. Codex is maker only and has not reviewed or ratified the remediation.
