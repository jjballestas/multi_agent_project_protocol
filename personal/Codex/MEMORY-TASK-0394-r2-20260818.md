# TASK-0394 remediation r2 implementation

- Maker commit `727d2289` defines generic payload membership structurally: every file below a
  top-level tree is required unless that tree is explicitly instance-only. Root-level protocol
  masters remain explicit. No filename suffix list remains.
- The Python and PowerShell twins now detect extensionless payloads and a removed `.githooks`
  export root; the permanent contract mutates each twin and requires `.githooks/pre-commit` in
  the failure output.
- The co-delivery contract now invokes `scripts/new_instance.py` as a subprocess with no `--tier`,
  exercising the real `main()` default coordination path, and requires both
  `skills/session-watchdogs.skill.md` and
  `scripts/harness/test_session_watchdog_filter.py` in the generated instance.
- The focused contract passed twice. Collaboration, encoding, and neutrality gates exited 0
  before commit. TASK-0394 remains `in_progress` under the r2 maker claim pending independent
  pre-delivery re-judgment; Codex has not reviewed or ratified the work.
