# TASK-0410 maker memory

- Implementation commit `96af63c6` restores Python/PowerShell neutrality parity.
- The extra Python-only exemption was for `claude` at `peer_mailbox_cron.ps1:555`; because
  `Claude` is no longer a configured identity term, the exemption was dead and was removed
  instead of copied into PowerShell.
- The three `runtime/context.py` exemptions were also dead: the configured identities no
  longer occur at lines 16-17, so both twins now omit those entries.
- The cardinal is derived from the independently executed PowerShell inventory, while actual
  source-line presence is checked from the Python inventory. No literal replacement cardinal
  was introduced.
- Both scanners now include `scripts/**/*.md` in required coverage and use case-sensitive glob
  behavior. Direct parity tests cover Markdown masters, case-varied excluded paths, and moved
  identity objects.
- The focused suite passed twice; falsification inventory 77/77, encoding, Python neutrality,
  and collaboration validation exited 0. Independent review remains required.
- Remediation r1 commit `b7bb0be1` makes exemption path keys, digest membership, and required
  glob membership explicitly ordinal in PowerShell. A real-scanner negative now proves that a
  case-varied path is rejected identically by both twins.
- Full-file census found no remaining unjustified case-insensitive membership operator. The
  remaining case-insensitive operations are intentional normalized token membership and
  case-insensitive finding regexes; structural, numeric, Boolean, and character comparisons do
  not create path/digest membership semantics.
