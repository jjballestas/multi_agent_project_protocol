# TASK-0414 remediation r3 memory

- Implementation commit `150ff371` adds the governed, material-free versioned event-auth key
  registry and binds verification to registered key existence, actor identity, and lifetime.
- Missing local material for a registered key is `unresolved_key` and non-fatal. Unknown ids,
  actor mismatch, use after `valid_through_seq`, and bad present signatures are fatal.
- The drift CLI now prints archive-inclusive boundary count and key ids. The focused fixture exits
  are registered history 0, unknown 1, bad present 1, own key as another actor 1, and late retired
  key 1. Independent review is still required.
- Delivery commit `86341c97` moves TASK-0414 to `in_review`, releases all Codex claims, and publishes
  the r3 handoff to Arquitecto. Clean-clone validation of implementation commit `150ff371` exited 0.
