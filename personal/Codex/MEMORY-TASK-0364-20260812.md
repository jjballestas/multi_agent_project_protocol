# TASK-0364 maker memory

- Implementation commit `cefd5e02` moves the canonical workflow jobs to the exact owned-runner
  labels from DECISION-0112: `falsification-runners` uses `protocol-win`; the other three jobs use
  `protocol-linux`.
- Each job publishes Python and pwsh versions; the Windows job also publishes Windows PowerShell
  5.1. Persistent workspaces are inspected before checkout, dirty state is logged and remediated,
  and an always-running final step leaves the workspace clean.
- The original step inventories remain ordered and complete: 7/7, 80/80, 3/3, and 5/5 original
  steps. A one-job rollback changes only its `runs-on` line.
- The workflow-derived dependency gate passed with the self-hosted labels. Collaboration,
  encoding, neutrality, and diff gates exited 0. A full pre-commit clean-worktree replay exceeded
  the 20-minute outer timeout before producing its final report; exact live-run dirty/clean and
  step-balance evidence remains required before governed delivery.
- TASK-0364 remains `in_progress` under the Codex maker claim. Codex has not reviewed or ratified
  the implementation.
- Live run `31595096498` exposed the Windows service policy: the default Windows PowerShell wrapper
  could not dot-source the generated step script because script execution is disabled. Commit
  `f23ef6a7` changes only the Windows job wrapper to installed `pwsh`; the falsification runner still
  exercises Windows PowerShell 5.1 internally and the version step publishes both hosts.
- Live run `31595924313` then exposed Git safe-directory protection because the Windows service
  runs as Network Service while the retained workspace was owned by the operator account. Commit
  `6b47e146` applies `safe.directory` only to each bounded cleanup command, without persistent host
  configuration or a service identity change.
- Live run `31596823928` proved dirty remediation and checkout on Windows, then exposed that
  `actions/setup-python` invokes a downloaded `setup.ps1` through Windows PowerShell and is blocked
  by the same host execution policy. Commit `6aee19ac` retains the `Set up Python` step but verifies
  and uses the already provisioned host Python, matching the successful self-hosted probe.
- Delivery commit `0cc51e21` moves TASK-0364 to `in_review`, releases both Codex claims, and
  publishes the self-contained Arquitecto handoff. Dirty run `31596823928`, clean follow-up
  `31597752400`, and exact-head real/clean comparison at `2eae1c39` provide AC2/AC3 evidence.
  Run `31630955323` exercised all four owned-runner jobs and its timing endpoint returned an empty
  billable map. Collaboration, encoding, neutrality, and diff gates exited 0 before delivery.
  Independent checker review remains required; Codex did not review or ratify the work.
