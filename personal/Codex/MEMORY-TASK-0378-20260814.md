# TASK-0378 implementation

- Commit `6f0feb3b` makes product commits require an active, file-scoped claim owned by the Git commit actor.
- `scripts/check_commit_trailers.py` binds `Task-Id` to the actor's active task claim; `.githooks/pre-commit` rejects earlier when no actor-owned claim covers every staged product path.
- Both gates derive the instance prefix and were executed with empty and `Aegis/` prefixes.
- Executed negatives returned exit 1 for no claim and another actor's claim in both gates; own active claims pass, while coordination-only `Task-Id: none` plus `Ops-Reason` remains exempt.
- Collaboration, encoding, neutrality, falsification inventory 75/75, compile, diff, and both hook suites exited 0 before commit.
- Codex is maker only; TASK-0378 still requires governed delivery to `in_review` and independent Analista review.
