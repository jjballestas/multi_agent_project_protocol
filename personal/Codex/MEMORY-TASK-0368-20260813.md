# TASK-0368 implementation memory

- Implementation commit: `94aa4ca3`; governed delivery commit: `dec16f53`.
- Decision currentness is now the property `superseded_by` is absent, independent of the historical
  `status` spelling. The attested policy maps current to `active` and explicitly superseded to
  `superseded`.
- The fast I4 gate and database builder consume the same property function.
- Permanent negative `NEG-MEMORY-CURRENT-DECISION-PROPERTY` derives cited decisions from an AGENTS.md
  fixture, kills the old status-literal mutant, exercises a third status spelling, proves dirty policy
  edits are ignored, and preserves present-versus-absent decision behavior.
- The 73-test memory suite, fast drift gate, collaboration validator, encoding scan, and both
  neutrality scans exited 0. TASK-0368 is `in_review`; both Codex claims are released and the
  self-contained handoff requests independent Analista review through Arquitecto.
- Codex is maker only and has not reviewed or ratified the work.
