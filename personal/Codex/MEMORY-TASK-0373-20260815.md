# TASK-0373 implementation memory

- Commit `e74109b4` implements F2 dry-run cold selection, canonical hot/cold rules, stable stub,
  pack-manifest and manifest-index renderers, plus five focused behavioral/golden tests.
- The complete memory suite passed 78/78. A detached clean worktree passed collaboration,
  encoding, neutrality, compile and the five focused tests. Clean `--propose-cold` exited 0,
  proposed 273 candidates, marked all 273 for stubs, and left porcelain unchanged.
- The live tree proposed 272 because one otherwise eligible path was under an active claim.
- No cold artifact moved, no archive content was created, and the DB retained zero cold packs.
- The implementation was initially held `in_progress` until its governed delivery could be
  committed atomically because the live ledger was co-dirty with pre-existing TASK-0367 state.
- Commit `063b92ed` checkpoints the Codex-signed TASK-0367/TASK-0373 ledger and matching task
  artifacts as one valid snapshot. TASK-0373 can now proceed to an atomic in-review delivery.
- Delivery commit `13c2c8a7` moves TASK-0373 to `in_review`, releases both maker claims, and
  publishes the self-contained Arquitecto handoff. Independent Analista review is required;
  Codex did not review or ratify its own implementation.
