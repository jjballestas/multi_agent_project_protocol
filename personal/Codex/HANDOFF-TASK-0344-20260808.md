# HANDOFF TASK-0344

Maker: Codex

Implementation commit: `dc34ca39`

## Diagnosis recorded before the fix

`personal/Codex/DIAG-TASK-0344-before-fix-20260808.md` records the measured branches. Both fixture
messages are resolved FYIs (`requires_response: false`, `status: answered`), so
`requires_unresolved_response()` returns false for both. The failure occurred earlier:
`assess()` returned `due=False`, and `apply_prune()` returned its intentional no-op without calling
`prune_mailbox()`.

Production is correct. TASK-0273 commit `3062214d` introduced the due-assessment guard; the older
mailbox fixture retained unreachable 99-percent thresholds and became stale.

## Delivered behavior

- The fixture is explicitly due (`cold_start_tokens_hard: 0`) and therefore exercises mailbox
  pruning instead of a maintenance no-op.
- `NEG-MAILBOX-PRUNE-MUST-ARCHIVE` mutates away the production `prune_mailbox()` call and proves
  that the expected old answered message then remains unarchived.
- All runner scratch uses the declared Aegis scratch umbrella.
- `scripts/prune_state.py` is unchanged.

## Missing task-level wiring

At `b726cce3^` (before TASK-0344 existed), `git grep` finds no task file containing
`examples/mailbox_status_cases/run_mailbox_status_cases.py`. The runner was wired in CI but absent
from every task `verification_cmd`. Future tasks that alter mailbox status/folder rules or
`prune_mailbox` behavior should include:

    python examples/mailbox_status_cases/run_mailbox_status_cases.py

## Evidence

- GitHub Actions run `31267480822`, step `Run mailbox status validation cases`: `success`.
- Exact clean clone at `b1d7d5bd`:
  - mailbox status runner: exit 0;
  - `prune_state.py --check`: exit 0;
  - collaboration validator: exit 0;
  - encoding scan: exit 0;
  - neutrality scan: exit 0;
  - `git status --short`: empty.
- Local falsification inventory: 63/63 contracts and 11/11 runners.

The overall Actions run is red only after this step, in the separately scoped neutrality runner;
the parallel falsification job retains the separately assigned TASK-0343 failure. Codex has not
reviewed or ratified this work.
