# Memoria Analista -- TASK-0397 r4 (2026-08-19 01:05 local)

## Que entregue

Commit `4a4b7040` (pusheado a `origin/main`), sobre HEAD previo `1e1c6555`:

- `Area_comun/artifacts/Analista-TASK-0397-r4-censo-derivado-verdict.md`
- `Area_comun/mailbox/open/MSG-20260819-Analista-to-Arquitecto-REVIEW-TASK-0397-r4-veredicto.md`

Veredicto: **OK-CLOSABLE** para el AC4 de TASK-0397, anclado en el PAR `83efdca1` + `bfeb4789`.
Vehiculo: TASK-0421 (alcance `Area_comun/artifacts/`), que esquivo la inanicion por
`active_external_claim` que mato la r3 sin ejecutarse.

## La leccion tecnica de esta review

**Un censo "re-derivado" no prueba nada por si solo: hay que perturbar la SEMILLA.** Un
`contracts=77` escrito a fuego habria "re-derivado" 77 igual de bien. El corte discriminante fueron
cinco perturbaciones sobre `bfeb4789`, cada una moviendo una coordenada distinta:

    P1 mas 1 contrato/2 fronteras -> 78/359/12 exit 1
    P2 vaciar 1 fichero (1c/3b)   -> 76/354/11 exit 1
    P3 mas 3 fronteras            -> 77/360/12 exit 1
    P4 id duplicado               ->  0/  0/ 0 exit 1
    P5 esquema roto               ->  0/  0/ 0 exit 1
    restaurado                    -> 77/357/12 exit 0

P1/P3 prueban de paso que censo y veredicto salen de **la misma invocacion**. P4/P5 cierran el
escape que fui a buscar ("la validacion cae y el censo sigue cantando 77"): no existe, se va a
0/0/0. **El censo no puede sobreestimar.**

**Trampa de la que casi me como el falso negativo:** mi primer intento de perturbacion inserto la
fila buscando `orig.index('[')` -- pero la lista se declara con **parentesis**
(`FALSIFICATION_CONTRACTS = (`), asi que inyecte el dict dentro de una anotacion de tipo. Resultado:
el censo NO se movio y el gate salio 0, lo que parecia un cardinal escrito a fuego. Era mi
instrumento el que no tocaba la semilla. **Antes de acusar al codigo, comprobar que la perturbacion
llego donde creo.** Corolario de "mutar produccion, no al runner": tambien hay que mutar la SINTAXIS
real, no la que supongo.

## El hallazgo que corrige el registro

La instruccion del Arquitecto afirmaba que el censo subio de 75/351 a 77/357 "porque `96af63c6`
anadio contratos entre tu medicion y la entrega". **Falso, medido con mi recuento AST commit a
commit** (`git rev-list --reverse A..B` + recuento en cada uno):

- `96af63c6` anadio **cero** contratos (77/357/12 antes y despues) y es **ancestro** de `83efdca1`.
- Los escalones reales: `2636eb9a` (16-ago 03:46, 75->76) y `0b942c09` (17-ago 09:03, 76->77).
- El gate corrido **en `83efdca1`** ya emitia 77/357/12 mientras el doc de ese mismo commit decia
  75/351. **El cardinal transcrito ya era falso en su propio commit de entrega, 32h tarde.**
  `bfeb4789` (+8 min) lo corrigio: **ese** es el commit que hace verdadero el AC4.

Generalizable: **cuando alguien explica un desfase por una causa concreta, la causa se verifica con
`merge-base --is-ancestor` y con una serie medida, no con la cronologia narrada.** Aqui la
explicacion era plausible y ordenada -- y no resistio un recuento de dos minutos.

## Residuo que recomende como tarea propia (R1)

El `.md` guarda una **copia transcrita a mano** del censo y **ningun gate la compara** con la salida
del checker. Hoy es correcta; la proxima adicion de contratos la deja obsoleta **en silencio**. Ya
paso una vez (en `83efdca1`) y solo se cazo porque el maker estaba mirando ese parrafo.

## Gates y numeros de corrida (todo en clon limpio `git clone -s`)

    check_falsification_contracts  bfeb4789   exit 0  x2 byte-identicas (+ --inventory exit 0) + 9 perturbadas
    check_falsification_contracts  83efdca1   exit 0  x2 byte-identicas
    check_falsification_contracts  6dc15cc4   exit 0  x1  (control: 0 lineas de censo)
    validate_collaboration_state   1e1c6555   exit 0  (clon limpio) y exit 0 (arbol caliente con mis 2 ficheros)
    scan_encoding                  1e1c6555   exit 0
    scan_domain_neutrality         1e1c6555   exit 0
    drift                          1e1c6555   has_drift=False

Scratch: `D:/Aegis_Scratch/protocol/an0397r4/{deliv_bfe,deliv83,ctrl6dc,headcl,hist}`.

## Anomalia que senalo y NO toco (DECISION-0018)

El hook de commit avisa **PRUNE DUE** (`cold_start_tokens 31214 >= 20000`,
`released_ratio 92.31 >= 90`). La poda exige capability `orchestrator`: es del **Arquitecto**. CI es
la frontera dura. Lo dejo senalado aqui y en el reporte al operador; no lo ejecuto.

## Operativa que funciono

- **El vehiculo TASK-0421 resolvio la inanicion.** Rutear la review contra el `task_id` de la tarea
  auditada hereda sus `scope_routes` de CODIGO; con el maker trabajando en `scripts/` el defer de
  7200 s muere antes que su claim. Alcance por lo que la review **ESCRIBE** = `Area_comun/artifacts/`.
- `validate_collaboration_state` en el arbol caliente tarda **varios minutos**; `tail --pid` no
  bloquea sobre PIDs Windows en Git Bash. Lo que si funciona: lanzarlo en background escribiendo su
  `$?` a un fichero y esperar con un bucle en Python que sondea el fichero.
- Rutas `Area_comun/artifacts/` y `Area_comun/mailbox/` son **governed pero NO product** para
  `check_commit_trailers.py`: no exigen claim activo. Solo `scripts/`, `.githooks/`,
  `runtime/` (no `runtime/state/`) y `protocol.config.json` lo exigen.
