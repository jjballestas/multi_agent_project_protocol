# MEMORIA -- TASK-0410 r1 (vehiculo TASK-0422), 2026-08-22 01:55 local

Commit de mi veredicto: `7f61ebe0` (pusheado; `origin/main` == `7f61ebe0`).
Padre: `ecc9ab13`. Ancla del juicio: `b7bb0be1`. Control historico: `b7bb0be1^` = `e94b312f`.
Veredicto: **CHANGE-REQUIRED**.
Artefacto: `Area_comun/artifacts/Analista-TASK-0410-r1-la-caja-cerrada-y-la-coordenada-abierta-verdict.md`
Mensaje: `Area_comun/mailbox/open/MSG-20260822-Analista-to-Arquitecto-REVIEW-TASK-0410-r1-veredicto.md`

## Lo que decidi y por que

La r1 cumplio su mandato: cerro los dos escapes de CAJA (clave de ruta 254, digest 269), el
negativo nuevo muere contra el gemelo viejo, el censo cuadra 88 == 88 y las seis puertas salen
verdes. Bloquee igual, por un eje distinto de la MISMA terna.

## La leccion que me llevo: la terna tiene TRES ejes, y yo solo habia auditado dos

El inventario de exenciones se declara por **(ruta, LINEA, digest)**. En el veredicto anterior
censé las pertenencias por CAJA y liste dos lineas. El maker arreglo esas dos (y dos mas). Al
ejercer la familia entera en vez del ejemplo aparecio el tercer eje, intacto:

    scan_domain_neutrality.py:267    text.splitlines()            <- parte en VT/FF/FS/NEL/LS/PS
    scan_domain_neutrality.ps1:329   Get-Content -Encoding UTF8   <- no

Un U+000C antes de la linea 555 exenta de `scripts/harness/peer_mailbox_cron.ps1`: Python numera
556 y REPORTA (exit 1), PowerShell numera 555 y CALLA (exit 0). Cinco separadores lo reproducen.
Es heredado (el control lo hace igual) y ningun test lo cubre; la suite entera sale exit 0 con el
escape vivo.

**Regla operativa:** cuando un control se ancla en una TUPLA, auditar cada componente por
separado. Arreglar dos de tres y declarar la paridad acreditada es exactamente lo que el AC3 de
esa tarea prohibe ("si solo falla uno, la paridad no esta acreditada").

## Segunda leccion: mi propio encargo enumero, y recibi la enumeracion

Yo escribi "sustituir la pertenencia insensible a caja ... para la clave de RUTA (linea 254) y
para el digest (linea 269)" y "un negativo: una ruta cuya caja difiere de la clave de exencion".
Recibi exactamente eso: dos lineas y un test que es literalmente mi sonda. Quedo fuera la
`.ps1:211` (`HashSet[string]` con `StringComparer::OrdinalIgnoreCase` frente al `set()` de
Python), que es la MISMA clase 45 lineas mas arriba. En el proximo lazo de arreglo escribo el
criterio de PERTENENCIA ("toda comparacion de pertenencia del gemelo PowerShell"), no la lista de
lineas.

## Tercera: el arreglo sin guardia se ve igual que el arreglo con guardia

`mut269` = clon del ancla con UNA edicion (revertir solo la comparacion ordinal del digest). Los
10 tests siguen verdes. El efecto estaba acreditado por sonda directa, pero nada lo ata: un
refactor lo reintroduce en silencio. **Por cada linea de arreglo, preguntar cual test muere si la
borro, y medirlo con un mutante de edicion unica.**

## Metodo que funciono y repito

- Causa probada por **edicion unica**: para SLIP-2 copie el `.ps1` del ancla y cambie solo el
  comparador de la 211 a `Ordinal`; el gemelo paso de 1 hallazgo a 2, iguales a Python. Eso es
  causa, no correlacion.
- 2x2 para el negativo nuevo: fichero de test del ancla **verbatim** + `.ps1` del control,
  verificado por md5 de las dos mitades. Murio por el `assertIn`, no por el `returncode` (esa
  mitad no discrimina: el gemelo viejo tambien sale 1 por hallazgos ajenos).
- Censo re-derivado por DOS caminos independientes (import del modulo Python vs ejecucion del
  `.ps1` con `-DumpIdentityInventory`) sobre un root PEQUENO -- el dump sobre el arbol entero
  tarda muchisimo y no aporta: el inventario esta cableado en el script, no depende del root.
- Gate ASCII estrenado con su NEGATIVO (un fichero con `\xc3\xa9` que da >0) antes de confiar en
  el 0 de mis dos ficheros.

## Trampas de esta corrida

- **El here-string de PowerShell (`@'...'@`) NO funciona en la Bash tool**: el commit entro con
  `@'` literal y el gate de trailers lo rechazo ("missing exact final trailer"). Solucion:
  escribir el mensaje a fichero y `git commit -F <fichero> -- <rutas>`.
- La notificacion de fin de un `run_in_background` reporta el exit del **wrapper**, no el del
  comando: el 2x2 salio "exit code 0" en la notificacion y `NEWTEST_ON_OLDPS1_EXIT=1` en el log.
  Gatear siempre por el codigo que yo escribo en el log.
- La suite tarda 420-540 s segun carga (10 tests). Dos corridas coincidentes en exit, numero de
  tests y los dos balances (`INVENTORY_MUTATION_BALANCE total=5 caught=5 escaped=0`,
  `TERM_MUTATION_BALANCE expected=623 current=623 mutant=534 losses=89`).

## Estado que dejo

- `TASK-0410` sigue `in_review`, owner Codex, reviewer yo. Mi veredicto pide r2 y avisa que es la
  SEGUNDA de las dos iteraciones que declare: si no cierra, escala al operador.
- `TASK-0422` (vehiculo) funciono: arranque sin diferirme. El alcance por lo que la review
  ESCRIBE (`Area_comun/artifacts/`) es el rodeo correcto hasta que aterrice TASK-0387.
- CERO claims activos al commitear; `origin/main` fast-forward limpio.
- **Poda vencida y sin hacer**: el hook aviso `cold_start_tokens 35275 >= 20000`. No es mio
  (requiere el checkpoint del Arquitecto); lo dejo senalado.
