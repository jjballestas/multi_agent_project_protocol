# MEMORY -- TASK-0394 r1 (2026-08-18)

Veredicto: **CHANGE-REQUIRED**. Commit `5d1eeff5`, empujado a `origin/main`.
Artefacto: `Area_comun/artifacts/Analista-TASK-0394-r1-el-control-ya-enrojece-y-la-co-entrega-no-verdict.md`.
Mensaje: `Area_comun/mailbox/open/MSG-20260818-Analista-to-Arquitecto-REVIEW-TASK-0394-r1-veredicto.md`.

Ancla juzgada: `50532ee7` + `f76cb9c0`. `origin/main` era `17461bfe` al clonar y se movio a
`fcf750b2` durante la revision (arbol compartido, HEAD local avanzo solo por commits de otra
sesion). Reverifique que `f76cb9c0` seguia siendo ancestro antes de firmar.

## Lo que decidio el veredicto

El frente 1 SI quedo arreglado: el control ya recorre el arbol, el minuendo no depende de `globs`,
y la celda que el instructor exigio (`tools/exportable.py` en raiz nueva) sale exit 1 en los dos
gemelos. El frente 2 tambien, en el EFECTO: instancia real de tier por defecto con guia y prueba,
y el comando citado por la guia corre exit 0 dentro de la instancia recien nacida.

Lo que lo tumbo fue el GUARDIA, no el efecto: `test_upgrade_instance_contract.py` llama
`module.copy_peer_harness(ROOT, generated)` directamente. El arreglo real vive en `main()` de
`new_instance.py`. Borre esas tres lineas de `main()` en una copia y medi: la instancia real pierde
la prueba y **el contrato sigue en exit 0**.

## Leccion que me llevo

**Cuando el maker declara un guardia de co-entrega, mutar el CAMINO DE PRODUCCION que el guardia
dice cubrir, no la semilla del guardia.** El patron "el contrato ata el helper, no el efecto" ya lo
tenia registrado, pero aqui llego disfrazado: el test SI hacia trabajo real (copiaba ficheros de
verdad, afirmaba una ruta de verdad), asi que leido no parecia vacuo. Solo la mutacion de `main()`
lo mostro. **Un guardia que ejerce el ayudante nunca prueba que alguien lo llame.**

Regla operativa: si el arreglo es "anadir una llamada X en el punto Y", el negativo valido es
*borrar la llamada en Y* y exigir rojo. Ejercer X aislado siempre sale verde.

## Segunda leccion -- la lista blanca de sufijos deja una clase de carga fuera

El maker respondio a "que hace adoptable a una raiz" con lista NEGRA de raices (bien, invierte el
default) + lista BLANCA de sufijos (`.py`, `.ps1`, `.skill.md`). Eso reintroduce el mismo tipo de
angulo muerto en otra coordenada. Prueba direccional que lo caza, mutando produccion y retirando
una raiz del conjunto cubierto:

    quitar "scripts"    -> exit 1 (62 ficheros)
    quitar "skills"     -> exit 1 (7)
    quitar "runtime"    -> exit 1 (31)
    quitar ".githooks"  -> exit 0 (0)   <- ciego: pre-commit y commit-msg no tienen extension

**Cuando un control filtra por sufijo/patron, comprobar que cubre TODO lo que el propio sistema
reparte hoy** -- inventariar las clases de carga reales (sin extension, `SKILL.md`, `.prompt.md`,
`.json`) antes de aceptar el filtro. El censo del arbol lo da en un comando.

## Tercera -- "line-identical" casi siempre es "set-identical"

El handoff decia que los reportes de los gemelos eran line-identical. Diff directo: distintos.
Comparados como CONJUNTO de filas: 123 = 123, `ONLY_PY` y `ONLY_PS` vacios. La causa es
`Sort-Object` de PowerShell (culture-aware) frente a `sorted` de Python (ordinal): `RUNBOOK-` y
`RUNBOOK_` caen en orden distinto. **Comparar gemelos por conjunto, no por linea, y corregir la
afirmacion aunque no sea bloqueante** -- el ledger no debe registrar algo mas fuerte que el hecho.

## Operativa que funciono

- Clon limpio `git clone -s` + copia del arbol sin `.git` como master de prueba: perturbar
  anadiendo/borrando un fichero y volver a correr es barato; no hace falta re-copiar 50 MB.
- El arbol esta COMPARTIDO y caliente: `origin/main` se movio dos veces durante la revision.
  Re-`fetch` justo antes de `git add` y de `git commit`, y **pathspec explicito tambien en
  `git commit ... -- <rutas>`** (habia un draft untracked de Arquitecto en `mailbox/open/`).
- Gate ASCII estrenado con su negativo (fichero con `\xc3\xa9` -> el gate lo ve, exit 1).
- Validador canonico tarda >120 s: lanzarlo en fondo o con timeout largo, nunca asumirlo.

## Rieles que deje puestos

Iteracion **2 de 2**. Re-juicio mio ANTES del commit de cierre. Si no converge, escala al operador
humano; no hay iteracion 3. Residuos declarados en el artefacto: R1 (sufijos), R2 (nada ejecuta el
contrato en CI y su `SCRATCH` fija ruta absoluta a `D:/`, incorrible en runners `protocol-linux`),
R3 (sin guardia de paridad de gemelos; ahora TRES literales duplicados), R4 (punto D, sucesora).
