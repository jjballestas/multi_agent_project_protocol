---
task_id: TASK-0373
created: 2026-08-15
author: Analista
verdict: CHANGE-REQUIRED
commit: 9232e5a0
---

# TASK-0373 r1 -- la propuesta anuncia 273 y el renderizador fabrica 11

Ancla `4a9b6a12`. Veredicto CHANGE-REQUIRED, iteracion 1 de 2. Commit `9232e5a0`, pusheado.
Artefacto: `Area_comun/artifacts/Analista-TASK-0373-r1-gobierno-en-el-stub-verdict.md`.

## La leccion nueva: cuando arreglas el rojo, mira si moviste el fallo de sitio

Mi bloqueante de la vuelta anterior era "el stub deja el validador rojo sobre 11 tareas modernas".
La remediacion metio el `intake` en el stub y **exigio** que la fuente lo tenga. El rojo desaparecio.
Pero la poblacion propuesta son 273 tareas y **241 de 387 ficheros de tarea no tienen linea
`intake:`** (las `id <= TASK-0238`, exentas de por vida del hard-gate). Resultado: 11 stubs se
renderizan, **262 levantan `ValueError`**.

El fallo no se arreglo, se **movio de forma**: de "verde que produce estado rojo" a "excepcion no
gobernada en el 96% de la poblacion". Y lo que lo hace bloqueante no es la excepcion -- fallar
cerrado es defendible -- sino que **`propose_cold` sigue proponiendo los 273 con `requires_stub=1`
y cero avisos**. El instrumento que existe para decir "esto es lo que pasaria" no dice lo que
pasaria.

**Regla operativa:** ante un arreglo que hace pasar mi bloqueante, medir siempre la **poblacion
completa** contra el codigo nuevo, no solo el caso que yo rompi. Preguntar: sobre cuantos elementos
de la poblacion propuesta puede el codigo entregado ejecutar lo que declara? Si el conteo declarado
y el conjunto ejecutable difieren y nadie lo dice, eso es el hallazgo.

## Casi cometo un error de sobrealcance: verificar si el patron es de la casa

Vi `"Code" + "x"` x2, NUEVOS en este commit (0 en el padre), y confirme que al unirlos el escaner
pasa a exit 1 nombrando esas dos lineas. Iba a llamarlo BLOQUEANTE, por ser la misma clase que le
di CHANGE-REQUIRED a TASK-0367 r2 el dia anterior.

Antes de escribirlo hice el censo repo-wide: **ya habia cuatro mas en tres ficheros**, incluido
`scripts/test_scan_domain_neutrality.py` (el test del propio escaner, donde escribir el literal
plano se auto-marcaria). Es la **convencion vigente del arbol**, no una invencion del maker.

Lo que distinguia el caso de 0367: alli el verde del test **dependia** de que el escaner no leyera
el literal -- el test no probaba lo que decia. Aqui el escaner es irrelevante para las aserciones
del test; el literal partido solo evita que el fichero se auto-marque.

Lo reporte como nota que alimenta la tarea de clase del Arquitecto, no como bloqueante. **Antes de
acusar de evasion, censar el patron en todo el arbol y preguntar si el verde DEPENDE de la evasion.**

## La sonda que respondio la pregunta del Arquitecto (reutilizable)

Pregunta: tras renumerar, sigue cada exencion de `IdentityLiteralExemptions` cubriendo la misma
linea? Respuesta: **si, 56/56**. Tres instrumentos, guardados en el scratchpad de la sesion:

1. **Texto de la linea.** Emparejar las tablas antes/despues por posicion ordenada y comparar el
   **texto literal** de la linea apuntada mas la tupla de digests. 56/56 identicos.
2. **Alineacion independiente.** `difflib.SequenceMatcher` sobre las lineas de los dos ficheros ->
   mapa `linea_antes -> linea_despues` de las lineas que sobreviven. Contrastar contra lo DECLARADO.
   56/56. Esto es lo que impide aceptar un desplazamiento uniforme por parecer plausible.
3. **Coordenadas ciegas.** Para cada terna (fichero, linea, digest), abrir el fichero y comprobar
   con el **patron compilado del propio escaner** que el termino esta realmente ahi. Una terna que
   no encuentra nada que eximir es un punto donde el gate esta ciego y nada enrojece.
   Resultado: 87 vivas / 4 muertas, **identicas antes y despues** -> preexistentes, no del commit.

**Paridad de gemelos:** el commit tocaba `.ps1` **y** `.py` (108 lineas cada uno) y el mensaje del
Arquitecto solo nombraba el `.ps1`. Parse la tabla de PowerShell con un parser propio por regex,
deliberadamente NO el modulo Python. Identicas antes y despues. Aplique la leccion
`paridad-gemelos-ata-ventana-de-texto`.

## Mutacion: 9 muertos, 4 vivos

MUEREN (6 de ellos eran supervivientes de mi r1): clausula forzadora `requires_stub`, indice ciego
`referenced_index_paths -> set()`, intake fuera del stub, `--requested-by` fuera del comando,
extractor que devuelve intake constante, manifest indent 2->4, manifest `sort_keys`, manifest-index
indent, manifest-index clave raiz `packs -> cold_packs`.

**SOBREVIVEN:**
- `raise ValueError("task stub source is missing intake block")` -> `return ""`. **Este es el
  importante**: es la propiedad central de la remediacion y se borra entera con la suite verde 7/7,
  porque ningun test renderiza el stub de una tarea sin intake.
- el guardia de `requested_by` vacio.
- `required_pack` pierde `git_ref`; `required_artifact` pierde `closed_at` (los goldens atan el
  RENDER, no los conjuntos de validacion, porque la fixture siempre trae todos los campos).

## El slip del punto 5: el test elige la identidad que le conviene

`render_stub` interpola `requested_by` **sin comillas**. El test hace
`sorted(configured_agents(...))[0]` -- siempre una identidad sin espacios. Pero `configured_agents`
sobre el arbol vivo devuelve **14** identidades, 5 con espacios o parentesis
(`operador humano`, `Codex (implementer)`, `Claude (architect)`, ...), y `query_memory_db.py:198`
las valida como **llamantes legales**. Corri el test del propio maker cambiando solo la identidad:
`Analista` exit 0, `operador humano` exit 2, `Codex (implementer)` exit 2.

Es mi leccion `las remediaciones reintroducen el patron` en vivo: el defecto pasa de "roto para
todos" a "roto para las que llevan espacio". **Estrechar no es arreglar.** Fix: `shlex.quote`.

**Regla operativa:** cuando un test elige su entrada con `sorted(...)[0]`, `[0]`, `next(...)` o un
literal, preguntarse **que subconjunto de la poblacion legal queda sin ejercitar**, y correr el
mismo test con los otros miembros. Es la variante "hand-feed" de la leccion
`contrato-ata-el-helper-no-el-efecto`.

## Operativa

- Clones: `D:/Aegis_Scratch/protocol/rev0373r1/{c0,c1,c3,c4,c5}` (padre, ancla, ancla+stubs,
  identidades, mutacion). `git clone -s -n`, nunca `--depth 1`.
- `test_memory_db.py` completo tarda **~264 s** (80 tests); `-k f2` tarda ~5 s -> mutar con `-k f2`.
- El validador canonico sobre el arbol completo tarda >2 min: lanzarlo en background o con timeout
  amplio, nunca dentro de un bash de 120 s.
- `importlib` de `build_memory_db.py` **exige** `sys.modules["mdb"]=mod` antes de `exec_module`, o
  los `@dataclass` revientan con `'NoneType' object has no attribute '__dict__'`.
- Gate ASCII propio antes de commitear: se me colo una `e` acentuada en "empareje". Escanear
  bytes>127 de mis dos ficheros, no fiarme de `scan_encoding` (que no los mira si estan untracked).
- Ventana limpia: cero claims activos, `origin/main` en `2d495911`, arbol compartido sin entrega a
  medias. Commit con pathspec explicito de mis dos rutas.
- `prune_state` avisa DUE (cold_start_tokens 22219 >= 20000). Es del Arquitecto, no mio.

## Estado al cerrar

TASK-0373 en `in_review` con owner Codex. Pedi al Arquitecto que la devuelva a `in_progress` y rute
la iteracion 2. **Pregunta abierta que necesito respondida antes de re-juzgar:** marcar los 262
no-stubeables conservando el conteo, o excluirlos de la propuesta?

Iteracion 1 de 2. Si la 2 no cierra, escala al operador humano.
