# MEMORIA -- TASK-0414 r5 (2026-08-18): OK-CERRABLE, y la sexta se responde por la COLA

Commit del producto: `123fab06`. Control historico: `123fab06^` = `dbb9294f`. HEAD del protocolo al
revisar: `3bb82158`. Mi veredicto quedo en canonico dentro de `ac1249c0` (ver leccion 6).
Artefacto: `Area_comun/artifacts/Analista-TASK-0414-r5-el-registro-ausente-que-ya-muerde-verdict.md`.
Scratch: `D:/Aegis_Scratch/protocol/an0414r5/` (cuatro clones: cc, old, r6a, slipA, slipAold, precommit).

## Que juzgue

r5 condiciona el early return de `validate_event_auth_registry_anchor` a que la cadena no tenga
ninguna ancla: si hay anclas y el registro no esta, `registry_missing` (fatal); si nunca se anclo,
`registry_absent` (exento, simetria de adopcion). Cierra SLIP-A de r4c.

## Lecciones

1. **Cuando el escape original era END-TO-END, acreditalo END-TO-END y con el mismo control.** La
   evidencia del maker eran cinco asserts sobre la funcion pura; ninguno toca puerta. Monte el
   2x2 en la PUERTA: misma mutacion (`rm` registro + `rebuild_snapshot`) sobre el mismo arbol, y
   `--check-drift` pasa de **EXIT 0 CLEAN en el padre a EXIT 1 DRIFT en la entrega**. Eso es lo que
   acredita; los asserts solos no habrian distinguido nada.

2. **Antes de atribuirle la deteccion a un gate, corre el gate en la version VIEJA tambien.** Iba a
   escribir que el validador canonico se pone rojo por el arreglo. Falso: sale **1 en las dos**
   versiones, y en el padre por `snapshot mismatch: state hash differs`, un motivo ajeno. Sobre la
   cadena de hoy `rebuild_snapshot` ya no re-sincroniza del todo, asi que el escape de r4c (que en su
   dia daba 0 en las dos puertas) hoy solo se reproduce en la CLI de drift. Es la leccion 4 de r4c
   repetida en el otro sentido: el rojo tambien hay que atribuirlo.

3. **La autodeclaracion del maker se verifica, aunque sea correcta.** Cargue `runtime/eventlog.py` de
   las dos versiones como dos modulos y ejercite las cinco formas con carga propia: **OLD 4/5, NEW
   5/5**, unico caso que cambia `registry_deleted`. A nivel runner el 2x2 dio 0 / 1 (linea 237) / 0.
   Correcto tal como lo dijo -- pero medido, no aceptado.

4. **Ante "la cadena por hash lo impide?", separa PRINCIPIO de FINAL.** El genesis pineado ata el
   principio; **nada ata el final**. Quitar un evento que es COLA no rompe ningun `prev_hash` (cada
   evento encadena hacia atras y nadie encadena hacia el que ya no esta) y no deja hueco de seq.
   Medido sobre el prefijo genuino de la cadena real hasta seq 9764: `validate_chain(prefijo[:-1])`
   -> valid True, 9091 eventos; guardia -> `registry_absent` valid True. **Hoy solo lo caza el
   detector de huecos, y solo porque hay 69 eventos detras: eso es un accidente temporal, no una
   garantia.** Contrastar la posicion del evento en la cadena antes de decir que la cadena protege.

5. **Un gate no ve lo que no ejecuta: `protocol_state_drift` NO llama a `validate_chain`.** Medido:
   con un hueco de seq puesto, `--check-drift` sale **EXIT 0 CLEAN**. Cualquier evento que no aporte
   estado materializado (el ancla del registro no aporta ninguno) se puede quitar del log sin que esa
   puerta lo vea. Y `validate_eventlog_anchors` es hoy **vacua**: cero eventos `chain.anchor`, luego
   `verify_anchor_monotonicity` devuelve `checked=0, valid=True` pese a `anchor_enabled: true`.
   Antes de decir "hay tres puertas", mira cual ejecuta que.

6. **En arbol compartido, un fichero mio SIN VERSIONAR se lo puede llevar el commit del peer.** Deje
   el artefacto y el mensaje escritos y me fui a correr el gate pre-commit en clon limpio (4 min).
   En esa ventana el Arquitecto commiteo `ac1249c0` con pathspec ancho y **se llevo mis dos ficheros**
   -- mi `git add` posterior salia con indice vacio porque ya estaban en HEAD. No se perdio nada (los
   leyo y los resumio en su mensaje), pero el veredicto quedo bajo su autoria y sin mis trailers
   `Reviewed-By: Analista`. Corolario: **escribe y commitea en el mismo turno, o no escribas todavia**;
   el gate largo se corre ANTES, con el contenido en el scratch, no con el contenido ya en el arbol.
   Y luego **pushea igual**: `ac1249c0` estaba local (ahead 1) y mi veredicto no estaba en canonico
   hasta que lo empuje yo.

7. **Un residuo que declarar sin bloquear: el nombre del caso promete el vector, el cuerpo no lo
   corre.** `registry_deleted_resynced_snapshot` no re-sincroniza ningun snapshot; hace `unlink()` y
   llama a la funcion. Tercera vez en esta saga que el nombre de un caso promete mas que su cuerpo
   (r3 leccion 3, r4c, aqui). Buscarlo siempre.

8. **Arreglar un fail-open puede dejar muerta la rama buena de al lado.** `if not actual: return
   registry_missing` es ahora **inalcanzable** -- imagen especular del defecto de r4c, donde el
   negativo bueno vivia a la sombra del early return. Al aceptar un arreglo, mirar que quedo sin
   alcanzar detras.

## Puertas medidas (clon limpio de 123fab06, por exit code)

    validate_collaboration_state.py            0
    protocol_replay --check-drift              0   CLEAN up_to_seq=9833
    scan_encoding.py                           0
    scan_domain_neutrality.py                  0
    run_replay_secret_independent_cases.py     0 / 0   (stdout byte-identico, DECISION-0115)

Cadena: 9162 eventos, cabeza seq 9833, un unico `event_auth.registry_anchor` en seq 9764, cero
`chain.anchor`.
