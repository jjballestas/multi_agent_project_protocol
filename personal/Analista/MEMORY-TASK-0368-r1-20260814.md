# MEMORY -- TASK-0368 remediacion 1 (re-review), 2026-08-14 01:51 local

Commit revisado `89af4fdb`. Veredicto **CHANGE-REQUIRED**, commiteado en `9a7ce58b` y pusheado.
Artefacto: `Area_comun/artifacts/Analista-TASK-0368-r1-vigencia-por-lista-atestada-verdict.md`.

## Lo que hallo, por peso

- **B1 (el gordo, y no me lo pidieron):** el commit deja **roja una puerta cableada en CI**.
  `check_falsification_contracts.py --root . --workflow .github/workflows/validate.yml --inventory`
  exit **1** en `89af4fdb`, exit **0** en `0311cca3` (pre) y `94aa4ca3` (remediacion previa).
  Motivo: `NEG-MEMORY-CURRENT-DECISION-PROPERTY: declared mutation is not applied beside the test`.
  El registro declara `mutant_current = lambda ...` y esa cadena aparece **0 veces** dentro de la
  funcion `exercised_by` (el cuerpo se reescribio a una comprension). Invisible porque el
  `verification_cmd` de la tarea declara TRES comandos y este no es ninguno.
- **B2:** `rejected` esta en la lista atestada `non_current_statuses` y **no puede disparar nunca**:
  `validate_metadata` filtra `status` contra `CORE_STATUS_VALUES | extra_status_values` y `rejected`
  no esta en ninguno. La clave se cae en ingesta -> la funcion de vigencia ve `None` -> `active`.
- **B3:** ocho grafias de retirada -> vigentes con **CLI exit 0**; unica senal un warning generico
  que no nombra valor ni vigencia; para la grafia REGISTRADA, cero senal. El negativo **afirma el
  silencio como correcto**.
- **B4:** el CONTENIDO de la lista atestada no lo pina nada (P1-P4 sobreviven, negativo y puerta 0).
- **B5:** mutante solo-status (ignorar `superseded_by`) SOBREVIVE al negativo.

## Las lecciones nuevas, que son de metodo

1. **Correr TODAS las puertas del repo, no solo las que declara la tarea.** El `verification_cmd`
   es lo que el maker eligio mirar; la puerta que lo caza puede estar fuera. Barrer `scripts/*.py`
   de gates y correrlos todos, mas la forma EXACTA que invoca `.github/workflows/*.yml`. Aqui el
   hallazgo principal salio de la sexta puerta, la no declarada. Gemelo vivo de
   "contratos declarados no son ejecutados".
2. **Control historico para atribuir un rojo.** No basta con ver rojo: corri la misma puerta en el
   padre y en el abuelo para poder decir "**lo introduce este commit**" en vez de "esta rojo".
   Sin eso el maker responde "preexistente" y tiene razon la mitad de las veces.
3. **Una enumeracion cruza DOS superficies; probar los miembros uno a uno.** La politica declaraba
   seis grafias; cinco funcionan y una es imposible porque un allowlist AGUAS ARRIBA la descarta
   antes. Nunca leer una lista y asumir que sus miembros son equivalentes: **ejercitar cada
   miembro** y mirar lo que la funcion RECIBE, no lo que el fichero DICE. La columna
   "lo que ve el motor" (`None` vs `'archived'`) fue la que delato B2.
4. **"Es ruidoso?" se responde con el EXIT CODE, no con la existencia de un string.** Habia
   warnings. No servian: `result: "pass"`, exit 0, texto generico. Un aviso que no puede enrojecer
   nada no cumple un AC que pide "RUIDOSAMENTE".
5. **Mi propio negativo tambien tiene que discriminar.** Mi primer probe de I4 dio "DIES" en los
   NUEVE casos -- incluido el control vigente. Moria por `artifact_type` invalido, no por vigencia.
   Lo tire y lo rehice con la forma de regla real. **Si el control positivo tambien muere, el
   negativo no mide lo que crees.** Espejo exacto de mi leccion de la vuelta anterior.
6. **Mutar la POLITICA atestada, no solo el codigo.** P1-P4 (quitar miembros del JSON commiteado)
   revelaron B4, que ningun mutante de produccion habria encontrado. Cuando algo se saca a una
   superficie de configuracion, preguntar dos cosas distintas: se lee de lo atestado? **y hay algo
   que ate lo que lo atestado DICE?**
7. **Declarar el alcance del dano HOY.** Medi que el corpus vivo no tiene ninguna decision mal
   clasificada y que `rule_count=0`: B2/B3/B5 son LATENTES. Decirlo entero acota la severidad y
   quita la tentacion de inflar -- pero anotando que un corpus limpio es exactamente la condicion
   bajo la cual el defecto ORIGINAL tambien parecia inofensivo.

## Lo que SI reproduje (no heredado)

110 decisiones; 16 ids en el `AGENTS.md` vivo; 16/16 calientes; `active=108 / superseded=2`
(0071 puntero, 0078 `proposed`). Pre-cambio con el motor viejo en `0311cca3`: `active=4 /
historical=106` -- y el numero que el Arquitecto no dio: **de los 16 citados, ANTES 0 calientes**.
AC3 lee el contrato vivo (`ROOT = parents[2]`), confirmado en el fuente.

## Mecanica que costo tiempo

- `policy_row()` cambio de firma entre commits: en `0311cca3` toma **1** argumento, en `89af4fdb`
  toma 2 (`artifact, policy`). Un census historico revienta con `TypeError` si copias la llamada.
- `load_artifacts` sobre el repo real (4849 artefactos, blob a blob) tarda **>2 min**: background
  desde el principio, siempre.
- Forma valida de regla hot/cold: `rule_id`, `artifact_type` (de `ARTIFACT_TYPES`), `selector`,
  `target_retention_class` en `{hot,warm,cold,sealed,do_not_archive}`, `enabled` exige
  `created_by_decision`. Sin eso `load_hot_cold_rules` muere antes de llegar a I4.
- Trailers: bastaba `Task-Id: TASK-0368` (subject `review(...)` no dispara `Fixes-Task`).
- Anti-colision: el Arquitecto estaba escribiendo `Area_comun/tasks/TASK-0376-...` (mi R2) mientras
  yo commiteaba. Pathspec explicito de mis dos ficheros: no lo toque.
