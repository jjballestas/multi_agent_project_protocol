---
agent_id: Analista
task_id: TASK-0373
created: 2026-08-15
---

# TASK-0373 F2 -- CHANGE-REQUIRED: la muestra estaba tomada de la mitad EXENTA de la poblacion

Ancla `17f36268` (implementacion `e74109b4`); HEAD al commitear `d1e8aaa4`. Veredicto
**CHANGE-REQUIRED** en `Area_comun/artifacts/Analista-TASK-0373-f2-enfriado-en-seco-verdict.md`,
mensaje `MSG-20260815-Analista-to-Arquitecto-REVIEW-TASK-0373-verdict.md`. Mi commit: `eda84611`.

## El hallazgo, y como salio

El stub de produccion sobre la ruta indexada de una tarea `done` REAL deja el validador canonico en
exit 1: `Task TASK-0350 missing intake block`. Restaurado el original, exit 0. Sobre los 273
candidatos de la propuesta viva: 11 errores de la misma clase.

**El test del maker usaba `examples/minimal_instance/TASK-0001`, que esta EXENTA del hard-gate de
intake por `id <= TASK-0238`.** La poblacion esta partida en dos mitades por una regla de gobierno
(el intake obligatorio para id > 0238) y la muestra cayo entera en la mitad exenta. No fue mala fe ni
descuido: `minimal_instance` es el sitio "natural" para una fixture de validador, y es justo el sitio
donde el gate no mira.

## Lecciones para la proxima

1. **Cuando una fixture vive en `examples/`, preguntar que EXENCIONES lleva ese ejemplo.** Un
   instrumento de referencia esta hecho para pasar; probar una propiedad contra el es probarla
   contra la parte facil del dominio. Aqui la frontera era un umbral de id.
2. **Correr la propiedad sobre LA FAMILIA que la regla selecciona, no sobre su ejemplo.** Aplicar el
   stub a los 273 candidatos costo 20 lineas de script y convirtio "un caso raro" en "11 errores de
   la clase que el AC prohibe". La propuesta ya me daba la poblacion: usarla.
3. **Preguntar si el VERDE discrimina, siempre.** `test_..._keeps_canonical_validator_green` pasa
   igual con un fichero de CERO BYTES en la misma ruta. El verde venia de que el fichero existiera,
   no del contenido del stub. Control barato, hallazgo grande.
4. **"Golden" que compara produccion contra produccion no es golden.**
   `assertEqual(rendered, render_pack_manifest(header,[artifact]))` prueba determinismo. Seis
   mutantes de formato sobre PRODUCCION (indent 2->4, sort_keys off, campos requeridos que
   desaparecen, clave raiz renombrada) sobrevivieron verdes. El del stub si tenia bytes literales y
   el control lo mato. **Leer la asercion, no el nombre del test.**
5. **Mutar produccion tambien para las propiedades que PASAN.** La clausula anti-B1 de
   `requires_stub` FUNCIONA (medido: 273/273 forzados con la regla en false) y aun asi se puede
   borrar entera de produccion sin poner rojo nada. Funcionar y estar probado son dos preguntas.
6. **Un dry-run se acredita con un censo de bytes, no con `git status --porcelain`.** Porcelain es
   CIEGO a lo gitignorado y `runtime/memory/` lo esta. Hice SHA-256 de los 5780 ficheros del arbol,
   ignorados incluidos: 0/0/0. Ahi AC3 pasa mas fuerte que su propio criterio.
7. **Buscar la entrada oculta que cambia la respuesta.** `--propose-cold` con selector
   `status=done&owner=Codex` da **0 candidatos con `runtime/memory/index.db` presente y 188 sin el**,
   exit 0 y sin warnings en ambos casos: el DB derivado solo expone 4 claves de metadata y cualquier
   otra clave del selector resuelve a "" en silencio. Misma regla, mismo commit, respuestas opuestas.
   Lo saque como tarea propia, no como remediacion: el selector ENVIADO no diverge (273 = 273), asi
   que es latente. **Separar lo que bloquea de lo que hay que registrar.**

## Mecanica que me costo tiempo

- **`git commit -- <ruta>` NO funciona con ficheros untracked**: "pathspec did not match any files".
  Hay que `git add` primero y commitear sin pathspec, verificando `git diff --cached --name-only`.
- **Un exec del peer me BORRO el artefacto del arbol compartido** mientras estaba untracked, entre el
  `git add` y el commit (el guard de residuos). Perdi el fichero y el staging. Regla nueva: en este
  arbol, **escribir el veredicto primero en area propia/scratch y copiarlo a la ruta gobernada en la
  MISMA invocacion que hace `add` + `commit`**. Lo deje senalado como residual 5 (DECISION-0018).
- El corpus se reindexa solo: la suite de tests CONSTRUYE `runtime/memory/index.db` (14 MB) en la
  raiz del clon. Esta gitignorado, asi que porcelain no lo ve -- y por eso el clon "limpio" tenia DB
  en la segunda mitad de mis medidas. Hay que saber si el DB esta o no antes de comparar conteos.
- `importlib.util.spec_from_file_location` revienta con los `@dataclass` de `build_memory_db.py`
  (`cls.__module__` None). Usar `sys.path.insert` + `importlib.import_module("build_memory_db")`.
- Gate por exit code REAL: `cmd | tail -5; echo $?` devuelve el exit de `tail`. Redirigir a fichero
  y leer `$?` inmediatamente.
- Trailer: bastaba `Task-Id: TASK-0373`. El pre-commit avisa de poda vencida y deja pasar el commit
  local (es tarea del Arquitecto, no mia).
