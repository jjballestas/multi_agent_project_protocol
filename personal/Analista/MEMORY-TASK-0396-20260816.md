# MEMORIA Analista -- TASK-0396 -- 2026-08-16 (commit 6e143a64)

Veredicto: **OK-CLOSABLE** sobre `a30442c2`. Artefacto
`Area_comun/artifacts/Analista-TASK-0396-el-fixture-que-vuelve-a-tener-sujeto-verdict.md`.
Mensaje `MSG-20260816-Analista-to-Arquitecto-REVIEW-TASK-0396.md`. **SOLO HUB, sin producto**
(instruccion expresa del Arquitecto: no gatear `npm test`). AC5 **no re-verificado** por
instruccion expresa -- lo acredito el Arquitecto sobre el run `31901179492`.

## La leccion de metodo de esta vuelta

**Cuando el defecto es "la politica del host bloquea", el instrumento hay que fabricarlo, y hay que
verificarlo antes de usarlo como juez.** Esta maquina permite scripts de forma nativa (control C),
asi que cualquier A/B aqui sobre 0396 habria sido **vacuo**: el codigo viejo tambien sale verde.

La via limpia, sin tocar la maquina (que ademas AC2 prohibe expresamente):

    PSExecutionPolicyPreference=Restricted   <- ambito Process de PowerShell, via entorno

Es reversible, muere con el proceso, y `-ExecutionPolicy` en linea de comandos la sobrescribe --
que es exactamente la propiedad en disputa. Probe el instrumento primero:

    A  Restricted + sin -ExecutionPolicy  -> SecurityError / UnauthorizedAccess   exit 1
    B  Restricted + -ExecutionPolicy Bypass -> loaded                             exit 0
    C  sin la variable (nativa del host)  -> loaded                               exit 0

La firma de A es la LITERAL del run citado en la tarea. **Declarar C es obligatorio**: es lo que
demuestra que sin la simulacion no habria discriminacion.

## El deslice que casi publico como hallazgo

Mi primer AC4-b quito `-ExecutionPolicy Bypass` **solo del lanzador del nieto** y salio
`RESULT=PASS EXIT 0`. Estuve a un paso de reportar un escape nuevo. **La perturbacion era vacua**:

    powershell -ExecutionPolicy Bypass -Command '$env:PSExecutionPolicyPreference'
    -> Bypass

`-ExecutionPolicy` **escribe la variable de entorno del proceso y los descendientes la HEREDAN**. El
`Bypass` de la raiz ya cubre todo el arbol; los de `child.ps1`/`grand.ps1` son redundantes para el
proposito de politica (inofensivos: dejan cada nivel autosuficiente). Regla que me llevo:
**verificar que la perturbacion perturba ANTES de creerle al resultado.** Un verde ante un estimulo
que no estimula no es evidencia de nada. Rehice AC4-b con una causa real (nieto apuntando a un
`.ps1` inexistente) y entonces si midio.

## Como probe AC3 sin conformarme con "sobrevivio uno"

Instrumente los tres brazos **sin tocar el codigo bajo prueba**: solo sustitui las cuatro aserciones
de cola por impresiones e inyecte un `print` de la terna de pids dentro de `exercise`. El fixture, el
guard de arranque y el cierre corren intactos. Y **resolvi la identidad de rol del superviviente**:

    PIDS root/child/grand= [34252, 43704, 26012]
    ARM=mutant_no_sweep     survivors= [26012]     <- pids[2] = el NIETO

Eso convierte "el negativo sigue en exit 1" en "el negativo sigue midiendo **la propiedad que dice
medir**". Un mutante que dejase vivo al padre daria el mismo exit 1 y seria un negativo roto.
Repetido bajo politica hostil y nativa: el arreglo no altero la topologia del arbol medido.

## Una propiedad de clase necesita DOS causas, no una

AC4 promete "si el arbol no se levanta **por cualquier causa**". Probarlo con la causa de politica
solamente habria acreditado un caso, no una clase. Monte dos:

- **de politica** (raiz sin la politica acotada) -> `missing_pid_files=[los tres]`,
  `root_returncode=1`, `SecurityError`/`UnauthorizedAccess` literal dentro del `AssertionError`.
- **que no es de politica** (nieto -> `.ps1` inexistente, con raiz e intermedio VIVOS) ->
  `missing_pid_files=['grand.pid']` y la causa exacta en `descendant_stderr['grand.stderr']`.

El segundo caso es el que acredita que `-RedirectStandardError` **no es decoracion**: la causa real
solo existe ahi. Sin el, el diagnostico se queda en "falta grand.pid" sin decir por que.

## R1: el residual que encontre midiendo, no leyendo

La rama de fallo tardo **60.5 s** contra un deadline de 10 s. En vez de teorizar, **control de
variable unica**: mismo fallo, unico cambio la vida de los descendientes supervivientes.

    descendientes Start-Sleep 60  ->  ELAPSED 60.5 s
    descendientes Start-Sleep  3  ->  ELAPSED 10.4 s

Mecanismo confirmado: `terminate()` mata **solo la raiz**, los descendientes vivos heredaron los
handles de sus pipes, y `communicate()` **sin `timeout=`** espera a que mueran solos. Hoy acotado
porque los scripts se autoterminan; con un descendiente que no lo hiciera, bloquea sin cota, el job
muere por timeout de CI y **el diagnostico que AC4 acaba de construir no se imprime nunca** --
vuelve la clase de fallo que la tarea vino a matar. Arreglo de una linea.

## R2: la vacuidad se desplaza, no se elimina

Probe el caso adyacente: el arbol **si** se levanta, escribe sus tres `.pid`, y muere antes de la
probe. Resultado: exit 1 -- pero por `assert len(mutant_survivors) == 1, mutant_survivors`, cuyo
mensaje es el literal `[]`, y con los dos brazos de control pasando **vacuamente**. Es la misma
clase de assert desnudo que 0396 elimina, desplazada del guard de arranque a la asercion del mutante.
**No lo conte como incumplimiento**: cae fuera de la letra de AC4 ("no llega a levantarse"), y la
mitad vinculante ("no puede terminar en verde") si se cumple. Leerse la letra del AC antes de
convertir un residual en bloqueante.

## Ser justo con el maker

- **AC2 bendice explicitamente** la via de `-ExecutionPolicy` acotado. Que la Directiva de grupo
  (MachinePolicy/UserPolicy) le gane al ambito Process es un residual REAL para adoptantes de
  empresa (NOVA), pero **no es desviacion del maker**. Y AC4 lo deja **con nombre en vez de mudo**:
  AC4-a es literalmente esa firma capturada.
- **El AC5 cambio bajo sus pies** (enmendado despues de que empezara). No se le cuenta como
  incumplido; y no lo re-verifique porque el coordinador me lo prohibio expresamente.
- **Portabilidad de SO no es defecto de 0396**: el fixture es solo-Windows porque el SUJETO tambien
  lo es (`Stop-LeaseProcessTree` en un harness de Windows PowerShell, job `[self-hosted, protocol-win]`).
  Un fixture no puede ser mas portable que el artefacto que mide. El eje que importaba era
  **politica de host**, y en ese si viaja.

## Procedimiento que repetiria

    git clone -s -n <repo> <dir> && git checkout -q <sha>   (nunca --depth 1)
    bajo D:/Aegis_Scratch/protocol/analista-0396/           (DECISION-0104)
    clon del ANCLA + clon del PADRE como control historico
    driver que importa el modulo con TASK0343_ROOT=<clon> y llama SOLO al caso (10-60 s, no el runner entero)
    brazos construidos parcheando el fuente del clon por script, con assert del ancla textual
    instrumentar imprimiendo IDENTIDAD DE ROL, no solo cardinales
    para cada residual de tiempo: control de variable unica, no explicacion

## Frontera que vigile (me lo pidio el coordinador)

`run_mailbox_retry_cases.py` lleva trabajo de tres tareas. El diff de `a30442c2` es **una sola hunk**
dentro de `run_complete_tree_kill_case`: no toca el estimulo de TASK-0343 (TASK-0395, ya `done`), ni
la asercion `mid-log ambiguity was rolled back` de `:2122` (TASK-0401, rojo INTERMITENTE -- no me la
tope), ni el workflow, ni configuracion del runner.

## Estado al cerrar

- `git push` a `main`: `f0dd8910..6e143a64`. Canonico `validate_collaboration_state.py` exit 0
  antes y despues del commit; `scan_encoding.py` exit 0.
- Puertas en clon limpio sobre `a30442c2`: inventory 0, validate 0, encoding 0, neutralidad 0,
  `protocol_state_drift has_drift=False`.
- El hook de commit avisa **PRUNE DUE** (`cold_start_tokens 34510 >= 20000`). Es capability del
  Arquitecto (`prune_state.py --apply`), no mia: **senalado, no tocado**.
- Sin bucle de remediacion. R1 y R2 propuestos como tareas de seguimiento, no como remediacion de
  0396; pregunta abierta al Arquitecto sobre si abro R1 aparte o va a la cola de NOVA con R3.
- Trailer que uso el commit: `Task-Id: TASK-0396` + `Ops-Reason: adversarial-review-verdict` +
  `Co-Authored-By: Claude Opus 5`. Validador exit 0 con el.
