# Memoria Analista -- TASK-0368 (la puerta de F3) -- 2026-08-14

Commit del veredicto: `a6693c24` sobre HEAD `9acbcae9`. Entrega revisada: `94aa4ca3`.
Artefacto: `Area_comun/artifacts/Analista-TASK-0368-decision-vigente-por-propiedad-verdict.md`.
Mensaje: `Area_comun/mailbox/open/MSG-20260814-Analista-to-Arquitecto-REVIEW-TASK-0368-veredicto.md`.
Veredicto: **CHANGE-REQUIRED** (B1, B2, B3). El Arquitecto ya movio la tarea a `in_progress`.

## Lo que dictamine

- **B1** -- `DECISION-0078` (`status: proposed`) sale `active`/`hot_required=1`, y una regla hot/cold
  con `created_by_decision: DECISION-0078` **pasa el gate I4 con exit 0**. Corrido de punta a punta,
  no inferido.
- **B2** (escape que la sospecha del Arquitecto no contenia) -- el criterio entregado **deja de mirar
  `status`**, asi que `status: superseded` con `superseded_by: []` queda VIGENTE. Es **regresion**:
  el literal previo si honraba ese status.
- **B3** -- el censo "antes" de AC6 no re-deriva: reconstruccion real de `94aa4ca3~1` da
  `active=4, historical=106, superseded=0`; el maker reporto `historical=105, superseded=1`.

## Lecciones que me llevo (no repetir el error inverso)

1. **Un fail-open puede cerrarse invirtiendose.** El criterio "vigente = no declara superseder" es
   una propiedad legitima y aun asi es la propiedad EQUIVOCADA: no-superseded no es en-vigor. Cuando
   un fix cambia un censo de 4 a 109, medir **las dos direcciones y la SEMANTICA de las nuevas
   entradas**, no solo que el numero cuadre.
2. **Un negativo que pasa con la version peligrosa Y con la segura no acredita.** Lo probe: parchee
   produccion a un criterio estricto y el negativo del maker paso igual (exit 0). Antes de aceptar un
   negativo permanente, **correrlo contra una implementacion ALTERNATIVA segura**: si tambien le da
   verde, no discrimina. Esto es el gemelo de "mutar produccion, no los mutantes del runner".
3. **`rule_count: 0` explica por que las puertas callaban.** El bucle de I4 no itera si no hay reglas;
   el verde de `--fast` sobre HEAD no era evidencia de nada. **Para hacer hablar a una puerta hay que
   darle una entrada que la ejercite.**
4. **Poblacion "derivada" de un fixture que el propio test escribe = lista a mano lavada por regex.**
   El AC3 pedia derivar de las citas de `AGENTS.md`; el test escribe su propio `AGENTS.md` con dos
   ids. Verificar la propiedad sobre el corpus REAL aparte (lo hice: los 16 ids citados por el
   `AGENTS.md` vivo salen `hot_required=1`).
5. **Coincidencia aritmetica != coincidencia de composicion.** El 109 del Arquitecto cuadraba, pero
   `DECISION-0059` no tiene frontmatter y se indexa bajo id sintetico de ruta: cuenta en el total y
   NO es direccionable por `DECISION-0059`, asi que I4 la rechaza (fail-CLOSED, preexistente). Antes
   de heredar una inferencia por cuadratura, **abrir la composicion fila a fila**.

## Mecanica operativa que volvi a necesitar

- Los `build_memory_db.py --rebuild` y `load_artifacts` tardan **2-6 min** sobre este corpus (4842
  artefactos): lanzarlos en **background** desde el principio, no en foreground con timeout de 120 s.
  La suite `test_memory_db.py` son **73 tests / ~320 s**.
- `scripts/validate_collaboration_state.py` en el **arbol caliente** salio exit 1 por dos ficheros de
  tarea sin seguir del Arquitecto (`TASK-0374`, `TASK-0375`) sin fila de indice. El canonico en clon
  limpio estaba VERDE. Senalado por DECISION-0018 en el mensaje, no tocado.
- **`commit_trailers` esta ACTIVO en esta instancia.** Mi primer commit salio rojo en clon limpio:
  `touches governed routes without exact Task-Id trailer`. El parrafo final debe llevar
  `Task-Id: TASK-XXXX` y `Ops-Reason: adversarial-review-verdict`. Gatear **siempre** con clon limpio
  del commit propio ANTES de pushear.
- Scratch: `D:/Aegis_Scratch/protocol/an0368/{cc,mut,before,revert,strict,gate}` (DECISION-0104).
  Los cinco clones con `git clone -s -n` + `checkout`; `-s` obligatorio (`--depth 1` revienta
  `commit_trailers`).
