# MEMORY -- TASK-0392 r2 (Analista, 2026-08-15)

Veredicto: **OK-CLOSABLE** en contenido sobre `2d6ad843`, **bloqueado el commit de cierre** por
`validate` exit 1 (TASK-0395 index='blocked' file='ready') en el ancla y en HEAD `44249e8d`.
Iteracion 2 de 2, cerrada: no pedi tercera. Artefacto:
`Area_comun/artifacts/Analista-TASK-0392-r2-enlace-guia-prueba-verdict.md`.

## Lecciones de metodo (las que valen para la proxima)

### 1. Mide el enlace guia<->prueba con mutantes SOBRE LA GUIA, y cuenta cuantos tokens vale

El Arquitecto muto una linea y sobrevivio. Yo mute cinco y sobrevivieron cuatro:

    M1 revertir la PLANTILLA de nombre a la forma pre-fix       exit 0  SOBREVIVE
    M2 borrar "every delivery must open one mailbox message"    exit 0  SOBREVIVE
    M3 paso 6 filtrando por AUTOR/co-autor, sin el literal      exit 0  SOBREVIVE
    M4 borrar la instruccion del nombre crudo                   exit 0  SOBREVIVE
    M5 quitar la clave WATCHDOG_COMMIT_TRAILER                  exit 1  portante

**La pregunta correcta no es "sobrevive este mutante" sino "cuantas propiedades del entregable lee
la prueba".** Respuesta aqui: DOS tokens. Un solo mutante que sobrevive parece una grieta; la bateria
revela que el enlace entero es de dos tokens y que la grieta es la regla.

### 2. Un negativo por TOKEN LITERAL no es un negativo por CLASE (mi propio fallo de spec)

Mi D1 pidio en prosa "que la prueba exija que la guia no prescriba descarte por autor/committer/
proveedor/modelo/co-autor" pero puso el GATE sobre la instancia ("la guia pre-fix + linea de contrato
-> exit != 0"). Codex implemento `OBSOLETE_GUIDE_TOKENS = ("<SELF_COMMIT_FILTER>",)`. El gate literal
se cumple (exit 1, verificado). La clase no: reescribi el paso 6 para prescribir filtrado por
identidad SIN ese literal y la prueba sale verde.

**Regla: el gate de un negativo se pone sobre una REESCRITURA, no sobre el texto viejo.** Es la
hermana de "el encargo que enumera recibe la enumeracion".

### 3. Una asercion puede medir en una COORDENADA donde el dato nunca vive

La prueba afirma cubrir "marca en el asunto" con
`f"subject Protocol-Monitor-Origin: {M}\x1fShared Actor\x1fclean body"` -- campo 0, que en el formato
real `%H\x1f%an\x1f%B` es el **hash**. Con commits REALES: el asunto es la primera linea del campo 2,
y ahi el caso **sigue filtrado**. Verde que certifica una propiedad que no existe.

**Metodo: no aceptar cadenas sinteticas para un contrato de formato; construir el dato con la
herramienta real (`git log --format=...`) y comprobar en que campo cae cada cosa.**

### 4. Prueba el gate aguas arriba, no solo el que arreglaron

B1 pidio que el PARSER fallara ruidoso. Lo hace, y para toda la familia. Pero medi con el glob que la
GUIA propone (`*-to-<ROLE>-*`), no con el `MSG-*` de la prueba, y descubri que lo que sigue callando
es el **glob**, un filtro aguas arriba del parser. La carga malformada que la prueba usa como
evidencia ni siquiera casaria el glob de la guia.

**Cuando arreglan un filtro, pregunta que hay ANTES de el.**

### 5. "Que recibe el adoptante" se responde GENERANDO la instancia, no leyendo el codigo

`copy_peer_harness` usa `shutil.copytree` del directorio entero -- leyendo el codigo pareceria que el
arnes viaja siempre. Generando instancias reales con `new_instance.py` en los tres tiers:

    coordination (POR DEFECTO)  guia PRESENTE  prueba AUSENTE  -> el comando de la guia da exit 2
    runtime                     guia PRESENTE  prueba presente
    attested                    guia en Aegis/ prueba presente

`COPIED_DIRS` incluye `skills`; el arnes solo lo copia `copy_runtime_tier_files`. **La guia y su
prueba se separan en el reparto.** Esto no se ve sin ejecutar el scaffolder.

### 6. El arbol caliente mintio OTRA VEZ, identico a r1

Mi `validate` de arranque dio exit 0; en clon limpio dio exit 1. Da verde porque un fichero de tarea
**sin commitear** hace coincidir fichero e indice. r1 = TASK-0367, r2 = TASK-0395. **Dos vueltas
seguidas, mismo mecanismo.** El clon limpio es lo unico que me separo del error las dos veces.

### 7. Cuando cerrar en vez de pedir iteracion 3

Cerre aunque encontre tres defectos nuevos, y el criterio fue: **ninguno hace falso el artefacto
entregado; hacen falso el grado de confianza que merece el verde.** Ninguno esta en la letra de
AC1-AC4, uno esta fuera de `scope_routes`, y dos nacen de imprecision de mi propia spec de r1.
Cobrarle al maker mi imprecision con una tercera vuelta que escala al operador es mal trato.
Van como tareas nuevas N-A / N-B / N-C.

### 8. Anti-colision: exec VIVO del peer -> no commitear, esperar

Al ir a commitear, `codex_mailbox_cron.exec-lease.json` tenia `state: running`, pid vivo, err.log
escrito hace 1s, `work_scope` sobre TASK-0395. Commitear mueve HEAD -> `ROLLBACK_DEFER
reason=head_changed` y le tumba el exec, que es justamente el que arregla el rojo que me bloquea a mi.
**Espere.** Verificar SIEMPRE el lease (no solo CLAIMS.json, que estaba vacio) antes de commitear.

### 9. Un peer me arrastro el borrador con `git add` ancho

`512c68d0` del Arquitecto (13:42:29) commiteo **mi fichero de veredicto untracked a medio escribir**.
Durante ~10 min el canonico llevo firmado por mi un veredicto que decia "el cierre no puede
aterrizar", ya falso. Lo corrigio mi `11978d65`. Senalado por DECISION-0018.

**Consecuencia para mi metodo: no dejar un veredicto a medias en una ruta compartida.** Escribirlo en
`personal/Analista/drafts/` y moverlo a `Area_comun/artifacts/` solo cuando este gateado y listo para
commitear en el mismo turno.

### 10. `head_changed` NO destruye el trabajo del peer -- lo lei mal al principio

Iba a aplazar mi commit una hora por un exec vivo de Codex. Mire el codigo:
`Restore-TransientExecResidue` es la rutina de **limpieza** (des-stagea el indice y pone en cuarentena
lo que creo el exec), y `ROLLBACK_DEFER reason=head_changed` hace que esa limpieza **se abstenga**.
Es una abstencion del rollback, no un rollback del trabajo.

**Leccion doble: (a) verificar la condicion en el codigo antes de pagar una hora de espera por ella;
(b) aun asi el lease se mira SIEMPRE -- `CLAIMS.json` estaba vacio y habia un exec vivo.**
