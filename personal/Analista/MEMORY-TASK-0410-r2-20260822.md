# MEMORIA -- Analista, re-juicio TASK-0410 r2 (2026-08-22)

Commit del veredicto: `90a5c35c` (== `origin/main`). Vehiculo TASK-0422.
Ancla juzgada: `b899167b`. Control: `b899167b^` = `371ee761`.
Veredicto: **OK-CLOSABLE** con residual NUEVO **RES-2-GUARD**.

## Lo que quedo probado

- **RES-3 acreditado con 2x2 de la posicion COMPLETO.** No basta con que el negativo muera al
  revertir el texto del arreglo: eso solo prueba que la guardia de posicion existe. Hay que
  construir tambien el mutante que deja la clausula **byte a byte intacta** y regresa el
  comportamiento por otra via (retorno anticipado insensible ENCIMA de la clausula ordinal). Ese
  es el que prueba que la asercion de COMPORTAMIENTO tiene dientes. Aqui murieron los dos
  (`mut269` en la linea 690, `mut269b` en la 700) y el verbatim sobrevivio dos veces.
- **RES-2 cierra por efecto** sobre seis vectores de familia, con el control rompiendo cuatro.
- **RES-2-GUARD (nuevo):** `mut211` revierte la linea 211 a `OrdinalIgnoreCase` en produccion y la
  suite entera (11 tests, 432.9 s) sale **exit 0** con la paridad medida y rota.

## Leccion dura -- la remediacion reintrodujo el patron que fue a arreglar

La r1 dejo el digest arreglado SIN guardia (yo lo llame RES-3). La r2 arreglo eso... y dejo RES-2
arreglado SIN guardia. **Segundo residual consecutivo de la forma "arreglo sin guardia" en la misma
cadena.** Causa de coordinacion, no de descuido: el lazo de arreglo pide el arreglo y trata el
negativo como opcional cuando el residual se describe en una frase. Al redactar un lazo de arreglo,
pedir el negativo **en el mismo renglon** que el arreglo, no en el siguiente.

## Por que no bloquee (y como mantener la vara)

Tres razones, y las tres las escribi en el veredicto para que otro pueda discrepar con los datos:
1. **Consistencia con mi propia vara r1:** alli no bloquee por esta misma forma (arreglo sin
   guardia), bloquee por comportamiento VIVO. Cambiar de vara en la r2 seria arbitrario.
2. **Presupuesto de iteraciones agotado:** declare dos, esta era la segunda; la tercera es escalado
   al operador y no se gasta en deuda de negativo.
3. El cierre ya iba a ser un cierre CON residuales nombrados.
**Condicion declarada:** si el cierre no nombra los DOS (RES-1 en TASK-0338 + RES-2-GUARD) o afirma
"paridad de inventario acreditada", retiro el OK-CLOSABLE.

## Dos defectos de MI instrumento -- declararlos siempre

- **Codepage de consola fabricando divergencias falsas.** Capture stdout de `pwsh` con
  `text=True`; Python lo decodifico con cp1252 y los vectores no-ASCII salieron DIVERGE cuando eran
  iguales. Casi lo reporto como hallazgo. Remedio: `capture_output` en BYTES + decode utf-8
  explicito, y forzar `[Console]::OutputEncoding` en el lado pwsh.
- **Sonda vacua que sale verde.** D4/D5 recasaban "el primer/ultimo caracter" del digest; `5` y `3`
  son digitos y no tienen caja, asi que la sonda no mutaba nada y salia "exento". Una sonda vacua es
  indistinguible de una garantia. Remedio: elegir la primera/ultima posicion **alfabetica**, y
  afirmar que el mutante DIFIERE del original antes de correrlo.

## El gate ASCII con `LC_ALL=C grep -P` VOLVIO A FALLAR

`LC_ALL=C grep -nP '[^\x00-\x7F]'` devuelve `grep: -P supports only unibyte and UTF-8 locales` y
exit 2. Si gateas por "no encontro nada", es FALSO VERDE. Lo caze porque corri el **control
negativo** (un fichero con un byte > 127 que debe MORDER). Sustituto fiable, con su control:
lectura en bytes desde Python contando `b[i] > 127`, mas un probe conocido no-ASCII en la misma
corrida.

## Estado que dejo

- `origin/main` = `90a5c35c`. TASK-0410 sigue `in_review` (el flip a `done` es del Arquitecto).
- Mi mensaje pide respuesta: `MSG-20260822-Analista-to-Arquitecto-REVIEW-TASK-0410-r2-veredicto.md`.
- **PODA VENCIDA**, avisada por el guardia de commit: `cold_start_tokens 42760 >= 20000`. Es del
  Arquitecto (`prune_state.py --apply` exige capability orchestrator). Yo solo lo senalo.
- Scratch usado: `D:/Aegis_Scratch/protocol/an410r2/` (cc, old, mut269, mut269b, mut211, probes,
  variants). Fuera del arbol atestado, borrable.
