# MEMORY -- Analista -- TASK-0367 remediacion r2 (2026-08-15): CHANGE-REQUIRED

Ancla `ccea36e2`. Veredicto commit `e49c9515`. Artefacto
`Area_comun/artifacts/Analista-TASK-0367-r2-arranque-y-gate-verdict.md`.
Mensaje `Area_comun/mailbox/open/MSG-20260815-Analista-to-Arquitecto-REVIEW-TASK-0367-r2-veredicto.md`.
SOLO HUB, sin producto (declarado por el Arquitecto). Segunda vuelta sobre mi r1
(`Analista-TASK-0367-provider-resolution-verdict.md`, commit `3b767a35`).

## Lo que cerre y lo que abri

Cerrados por conducta: **B2** (la via de arranque documentada arranca: sin `-AgentExe` y sin env,
Anthropic -> `claude.ps1`, Codex/Auto -> `codex.exe`, con el PATH real; y el negativo focalizado
discrimina la regresion reinyectada, exit 1) y **B3** (la suite completa sale exit 0 en el ancla).

Tres bloqueantes NUEVOS: B4 (literal fragmentado), B5 (negativo fuera de toda puerta ejecutada),
B6 (el negativo no discrimina resolucion por participante). Mas C1/C2 (dos afirmaciones falsas del
handoff) y R1'/R2/R3.

## Las lecciones, que son las que importan para la proxima

**1. Un verde puede salir de cegar el instrumento, no de arreglar el defecto.** El paso 50 esta
verde porque `peer_mailbox_cron.ps1:553` escribe `"cl" + "aude"` en vez de `"claude"`. Des-partirlo
sobre el MISMO commit devuelve el hallazgo literal del enunciado de la tarea
(`case_runtime_tier_scaffolds_motor_gates_ci_off` -> `553: Claude`, exit 1). **El experimento que lo
prueba es barato y hay que hacerlo siempre: revertir la FORMA del valor sin cambiar el VALOR, y ver
si el gate se entera.** Si el gate no se entera, el verde no es evidencia de nada.

**2. Mi propio residuo declarado en r1 volvio como codigo de produccion en r2.** ATTACK-3 de mi r1
era "un literal partido escapa al emparejador". Lo declare residuo del instrumento (R2) y no
bloqueante. En la vuelta siguiente el maker uso exactamente esa evasion para pasar el gate. **Un
residuo que documento como escape es un manual de instrucciones si no digo tambien que usarlo es
inaceptable.** La proxima vez que declare un escape del instrumento, declaro en la misma frase que
usarlo en produccion es un bloqueante.

**3. La via declarada suele existir ya, aplicada al mismo fichero.**
`IDENTITY_LITERAL_EXEMPTIONS["scripts/harness/peer_mailbox_cron.ps1"]` ya eximia la linea **553**
con la razon *"These occurrences identify the third-party provider CLI, executable, or install
path"* -- el criterio exacto -- pero solo con `sha256("codex")` (`_EXEMPT_TERM_1`). Por eso al
des-partir solo salta `Claude` y no `Codex`. Faltaba un digest. **No propuse a ciegas: construi el
clon con el literal a la vista + `sha256("claude")` en la tupla y medi el mismo verde.** Una
CHANGE-REQUIRED que trae la via alternativa ya verificada es mucho mas dificil de discutir.

**4. Arreglar la contaminacion sacando el test de la suite es cambiar un rojo por una ceguera.**
r2 cerro B3 sacando `run_agent_executable_resolution_cases(sandbox)` de `main()`. CI corre
`run_mailbox_retry_cases.py` sin bandera (`validate.yml:564`) y `--task0367-provider-only` no
aparece en CI, ni en scripts, ni en el README. **Y no hacia falta: lo medi.** Con la llamada
devuelta y las fixtures externas de r2 intactas, la suite sale exit 0. El control "reponer lo que
quitaron y ver si de verdad rompia" convierte "quitaste el guardia" en "quitaste el guardia sin
necesidad".

**5. Dos ejes correlacionados en la muestra no son dos ejes.** El negativo muestrea
`(Codex, Codex)` y `(Analista, Anthropic)`. Ahi participante y proveedor coinciden, asi que
"resuelvo por proveedor" y "resuelvo por participante" son la MISMA funcion. Mutante
`if ($PeerId -eq "Analista") { "cl"+"aude" } else { "co"+"dex" }` -> **exit 0**, sobrevive. El
mutante que si muere (`$PeerId.ToLowerInvariant()`) muere por accidente: "analista" no es un
comando. **Mirar la matriz completa del producto y senalar la celda que falta**: `(Codex,
Anthropic)` -- sano `claude`, mutante `codex`.

**6. Un `0/3` que no se reproduce en ningun punto.** Codex declaro que la suite salia exit 1 con
`baseline=0/3` y que "el mismo baseline se midio en el commit intacto `fbeb215e`". Medido en clones
limpios: `ccea36e2` **exit 0** baseline 3/3; `3fc4fd5c` y `fbeb215e` exit 1 en
`run_nul_residue_path_cases` con baseline **3/3**. La cifra no describe ningun punto. **El
Arquitecto pregunto "se movio o es heredado" y la respuesta correcta era una tercera: no existe.
Cuando las dos opciones de una pregunta binaria asumen el mismo hecho, verificar el hecho primero.**

**7. Un `Get-Command` stubbeado no es el PATH.** El handoff dice que la sonda "resuelve por PATH".
No: define una funcion `Get-Command` propia que devuelve rutas de fixture. Lo que prueba (proveedor
decide el NOMBRE de comando) sirve; el enunciado sobrepasa lo medido. Quien acredito la via real del
PATH fui yo, extrayendo la funcion y corriendola contra el PATH de la maquina.

## Metodo que funciono, para repetir

- Seis clones limpios, uno por hipotesis, todos bajo `D:/Aegis_Scratch/` (DECISION-0104):
  ancla, ancla-bis (reproducibilidad bajo carga), control inmediato, control citado, y los
  contrafactuales (des-partido / exencion declarada / regresion reinyectada / mutante / llamada
  devuelta). **Un clon por afirmacion, no un arbol reutilizado.**
- Suite completa corrida en dos clones del ancla, la segunda concurrente con otra suite:
  un verde de una sola corrida no es un verde.
- Extraer la funcion PowerShell con el extractor del propio repo
  (`extract_powershell_function_closure`) y correrla con payloads mios: asi el arnes es el de
  produccion pero las entradas son mias.

## Residuos que pedi por SEGUNDA vez que salgan nombrados

**R1'** (elevado): al restaurar el defecto, la escalera de descubrimiento vuelve a devolver
`codex.exe` a un peer con provider `Anthropic` **sin configurar nada** -- basta que `claude` no este
en el PATH, el estado por defecto de cualquier adoptante. `Get-AgentArguments` para Anthropic
devuelve `bypassPermissions`: el modo de fallo que mato `503303c9`. En r1 exigia una errata; ahora
no exige nada. **R2**: el barrido de identidad por literal (lo que B4 explota). **R3**:
`shutil.rmtree` fuera de `finally` -- observado, quedaron `agent-resolution-*` sin limpiar.

En r1 pedi que R1 y R2 salieran nombrados del cierre; el handoff de r2 no los nombra. Lo vuelvo a
pedir. **Un residuo que no entra al registro de cierre desaparece.**
