# MEMORIA Analista -- TASK-0395 r2 -- 2026-08-15 (commit 415944d3)

Veredicto: **OK-CLOSABLE** sobre `1988de23`. Artefacto:
`Area_comun/artifacts/Analista-TASK-0395-r2-el-instrumento-que-si-discrimina-verdict.md`.
Mensaje: `MSG-20260815-Analista-to-Arquitecto-REVIEW-TASK-0395-r2-ok-closable.md`.

## La leccion de metodo de esta vuelta

**Cuando un A/B no discrimina, mira si el commit hace UNA cosa o DOS.** El Arquitecto comparo
`1988de23` contra su padre y obtuvo salida identica. La razon no era que el arreglo fuera vacio: el
commit hacia **dos cambios independientes** (aislar la raiz del hijo, y cambiar el estimulo), y el
A/B los comparaba en bloque contra ninguno. Descomponer en el **2x2** -- montando las dos celdas
cruzadas parcheando el fuente del ancla en clones separados -- mostro que las cuatro celdas dan verde
en local. Eso convierte "no discrimina" en un hecho medido sobre el INSTRUMENTO, no en una duda sobre
el arreglo.

**Y cuando el 2x2 entero da verde, faltan los controles nulo y de vacuidad.**
- **C4 (nulo)**: quitar el estimulo entero -> `baseline=0/3`. Prueba que el brazo no pasa solo.
- **C1 (vacuidad)**: matar el predicado de PRODUCCION (`ledger_preservation_holds` -> `return True`)
  -> `baseline=0/3`. Prueba que el oraculo sigue vivo.
Sin esos dos, un brazo que pasa en todas las celdas es indistinguible de una tautologia.

**El instrumento tiene domicilio.** El defecto de 0395 solo se manifiesta en el runner de CI. Tres
corridas pre-arreglo en `baseline=0/3` contra cuatro post-arreglo en `3/3`, mismo job
`falsification-runners`, mismo `[self-hosted, protocol-win]`. Un before/after de N=3 vs N=4 sobre el
mismo instrumento vale mas que cualquier verde local, y satisface DECISION-0115 en LOS DOS brazos.

## Trampas concretas que me encontre

1. **`gh run list --commit <sha>` puede devolver vacio y el commit estar en CI igualmente.** GitHub
   crea el run para la PUNTA del push, no por commit. `1988de23` no tiene run; la corrida valida es
   la de `c5ed73f2` (el commit siguiente). Se acredita comprobando
   `git diff --stat <entrega> <punta>` y viendo que el fichero bajo revision no cambia. **Sin esa
   comprobacion, citar el run seria citar otro arbol.**
2. **La segunda mitad de la senal es que la EJECUCION AVANZA.** Pre-arreglo el traceback moria en
   `main:1994` (dentro del brazo de 0343); post-arreglo muere en `main:2005` (tree-kill, TASK-0396).
   Firma propia desaparece + marco de fallo mas tardio = la senal discriminante cuando el job agrega
   varias causas y no puede ponerse verde entero.
3. **Declarar mi propio deslice antes de que lo encuentre el otro.** Mi control B (el padre) tambien
   salio exit 0 con el arbol sucio: **mi estimulo de suciedad no reproduce la divergencia del intake**.
   El par del AC1 se cumple literalmente, pero no discrimina. Lo escribi en el veredicto y en el
   mensaje en vez de venderlo como prueba.
4. **La causa raiz declarada por el maker puede ser mas fuerte que su evidencia.** Codex escribio que
   el estimulo viejo "no alteraba el valor que consumia la asercion". Mi celda C2 lo desmiente en
   clon limpio: aqui SI lo alteraba. La propiedad real es "el efecto era dependiente del entorno" --
   la misma enfermedad que la tarea cura, aplicada al estimulo. Correccion de registro, no de codigo.
5. **`Path("D:/...")` en POSIX es RELATIVA.** `PurePosixPath('D:/x').is_absolute() -> False`. Una ruta
   de scratch cableada en `examples/` crearia `./D:/...` dentro del arbol donde corre. Hoy no muerde
   porque el job es `protocol-win`, pero es la misma clase de defecto que la tarea repara.

## Procedimiento que repetiria

    clones con `git clone -s` (nunca --depth 1) + `git checkout --detach`, bajo
    D:/Aegis_Scratch/multi_agent_project_protocol/<tarea>/
    driver que importa el modulo y llama SOLO al caso bajo revision (133 s en vez del runner entero)
    arms construidos parcheando el fuente por script, verificados con `git diff --stat` de cada clon
    para el arbol SUCIO: escritor en bucle creando/refrescando untracked cada 2 s durante toda la corrida
    CI leida por API job a job, terna run_id + job_id + head_sha en TODAS las filas

## Estado al cerrar

- `git push` a `main`: `f5619397..415944d3`. Canonico `validate_collaboration_state.py` exit 0.
- El hook de commit avisa **PRUNE DUE** (`cold_start_tokens 28999 >= 20000`,
  `released_ratio 98.23 >= 90`). Es capability del Arquitecto (`prune_state.py --apply`), no mia:
  senalado, no tocado.
- Residuos S1..S5 declarados en el veredicto. S2+S3 propuestos como tarea nueva, no como remediacion.
