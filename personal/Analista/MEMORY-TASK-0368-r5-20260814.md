---
agent_id: Analista
task_id: TASK-0368
created: 2026-08-14
---

# TASK-0368 r5 -- OK-CLOSABLE, un arbitraje, y una afirmacion mia que caduco en 13 minutos

Commit revisado `ecc1478e` (implementacion `f50ecff3`), HEAD al empezar `139d07e1`. Veredicto
**OK-CLOSABLE** en `Area_comun/artifacts/Analista-TASK-0368-r5-supersesion-sin-status-verdict.md`.
Cerro el Arquitecto en `2d2eeb4d`: TASK-0368 `done`, sucesora **TASK-0384** creada con mi residual R1.
Mis dos commits propios: `3ce60133` + `b47b8f74` (anomalia de hooksPath y su correccion).

## Lo que hice y por que salio ancho

    17 formas de superseded_by x 2 ramas   PASS  17/17 IDENTICAS
    e2e por load_artifacts -> policy_row   PASS  10 sondas commiteadas + 4 decisiones reales
    censo A/B dentro del MISMO commit      PASS  cero filas movidas en las dos direcciones
    mutantes contra el runner declarado    PASS  a/b/c en exit 1, control en 0, d equivalente
    seis puertas, clon limpio, dos rondas  PASS  6/6 x2

## Lecciones

1. **No revises la fixture: revisa la FAMILIA que el criterio promete.** La fixture probaba un punto
   (puntero escalar, status ausente). El criterio prometia que la rama evaluara el puntero. Corri 17
   formas en las DOS ramas y el enunciado fuerte no era "el escalar tambien funciona": era que **las
   dos ramas son la misma funcion sobre el puntero**. La paridad de ramas dice mas que cualquier
   ejemplo, y es la forma correcta de acreditar "esta rama ya evalua X".
2. **Para probar que NADA se movio: mismo corpus, DOS BRAZOS DE CODIGO.** Extension de mi leccion de
   r4 (no comparar conteos de dos commits). Aqui saque el modulo de `f50ecff3~1` con `git show` a un
   fichero y clasifique el **corpus de ecc1478e** con los dos modulos. `{active:111, superseded:2}`
   identico, cero filas movidas. El control y la sonda dentro del mismo commit tambien valen cuando
   lo que varias es el CODIGO, no los datos.
3. **Un mutante que sobrevive puede ser EQUIVALENTE: hay que demostrarlo, no suponerlo.** M-R5d
   (`x not in (None,"",[])` en vez de `value_list(x)`) sobrevivio. No es fuga: `validate_metadata`
   normaliza toda clave de relacion a lista de cadenas recortadas **antes** del clasificador
   (`value = values`), asi que las dos condiciones coinciden en todo el dominio ALCANZABLE, en los
   dos unicos llamantes. **El argumento que lo salva es de ALCANZABILIDAD**; sin ese argumento habria
   sido un hueco. Declararlo como equivalente en el veredicto, nunca callarlo.
4. **Un arbitraje se contesta atacando el ANTECEDENTE del criterio, no su espiritu.** Me pidieron
   dictaminar si un aviso mentiroso bloqueaba por AC4. AC4 dice *"si el criterio la clasifica mal,
   tiene que decirlo RUIDOSAMENTE"*. El antecedente es **una clasificacion erronea**, y no habia
   ninguna. Ademas publique las **tres condiciones falsables bajo las que SI habria bloqueado**
   (existe hoy un artefacto real del caso / la clasificacion es erronea / el aviso decide una
   puerta), las tres medidas y las tres ausentes. Un dictamen sin sus condiciones de reversion es
   una opinion.
5. **Nombrar la CLASE del residuo, no la cadena a parchear.** R1 no es "el texto del aviso esta mal":
   es **un mensaje que afirma un desenlace que no computa**, y vive en la frontera de carga donde el
   clasificador aun no ha corrido, por eso solo puede cablear el resultado. Con la clase escrita, la
   sucesora tiene criterio; con la cadena, tendria un parche. El Arquitecto abrio TASK-0384 con esa
   clase literal como titulo.
6. **Agrandar un lazo que el operador acaba de estrechar es el revisor decidiendo alcance.** Yo habia
   escalado, el operador autorizo UNA propiedad, y la propiedad estaba entregada y era la correcta.
   Convertir un residuo hallado despues en bloqueante habria sido yo fijando el alcance. Se declara y
   se abre aparte. (Y el coordinador hizo bien en NO rutearse a si mismo la sexta vuelta: seria el
   maker haciendo de checker.)
7. **UNA AFIRMACION SOBRE LA CONFIGURACION DE LA MAQUINA TIENE FECHA.** El fallo de esta ronda es
   mio. Medi `core.hooksPath = /tmp/task0364-poisoned-hooks` (directorio inexistente, hooks inertes) a
   las **15:28**, escribi un mensaje en PRESENTE y lo commitee; a las **15:41** ya estaba rearmado a
   `.githooks` -- me lo delato mi propio commit, que disparo la pre-commit. Publique una afirmacion
   viva falsa y tuve que corregirla en el commit siguiente. Es la familia de
   `citar-un-run-sin-abrirlo` **aplicada a CONFIG en vez de a runs**: re-medir el hecho de entorno
   **justo antes** de publicarlo, y escribirlo con su hora, no en presente.
   Lo que SI sobrevivio a la correccion: la ventana de gates inertes existio y no consta en ningun
   sitio, y el guard sigue ciego (punto 8).
8. **Un guard colocado donde el fallo es IMPOSIBLE.** `validate.yml:651-664` rechaza un
   `core.hooksPath` contaminado... sobre el **checkout de CI**, que es un clon fresco y por tanto
   siempre limpio. El envenenamiento solo puede existir en un arbol persistente, que es donde nada
   mira. Por eso el valor sobrevivio desde TASK-0364 hasta que lo encontro una lectura MANUAL mia.
   Clase reutilizable: **preguntar en que entorno puede materializarse el fallo y comprobar que el
   guard corre AHI**. Los tests de la hook arman su propio `hooksPath`, asi que salen verdes con
   independencia del arbol vivo: verde de test != gate desplegado (`harness-mergeado-no-es-desplegado`).

## Mecanica

- `git commit -F msg -- <ruta>` **falla si el fichero esta SIN SEGUIMIENTO**: `git add -- <ruta>`
  primero y despues commit con el mismo pathspec explicito. Mi nota de r4 decia "pathspec explicito"
  y se comio este paso.
- Cargar `build_memory_db.py` por `importlib` exige `sys.modules["mdb"] = mod` **antes** de
  `exec_module`, o el `@dataclass` revienta con `'NoneType' object has no attribute '__dict__'`.
- La clase del runner es `MemoryDbTests` (plural). Con el nombre mal, unittest da
  `FAILED (errors=1)` en `0.000s` y **el control tambien falla**: si el control no pasa, el
  instrumento esta roto, no produccion.
- `bash script.sh 2>&1 | tail -N` en background **no escribe nada hasta el final** (tail bufferiza):
  redirigir a fichero y mirar mtimes, o no ves el progreso.
- Sondas e2e: escribir ficheros de decision, **commitearlos** (load_artifacts lee blobs de git) en un
  clon aparte con rama propia. Suite completa ~5-6 min; el metodo declarado solo, ~9 s.
- **Arbol compartido:** el Arquitecto commiteo mis DOS ficheros dentro de SU commit `2d2eeb4d` (mi
  veredicto y mi mensaje, este ultimo directo a `archived/`) leyendolos del arbol **sin commitear**, y
  cerro sobre ellos. Salio bien porque mi dictamen era OK-CLOSABLE, pero el cierre se decidio sobre un
  artefacto no publicado. No es reparable a posteriori; se anota.
- Scratch bajo `D:/Aegis_Scratch/mapp/` (DECISION-0104): `t0368r5` (seis puertas x2), `e2e5` (sondas
  sobre corpus real), `mut5` (mutantes), `hookprobe` (par ARM A/ARM B), `vfy5` (clean-clone de mi HEAD).
