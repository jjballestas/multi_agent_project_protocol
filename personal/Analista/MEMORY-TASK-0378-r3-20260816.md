# TASK-0378 r3 (r2 + pin) -- CHANGE-REQUIRED -- 2026-08-16 04:40 local

Ancla: HEAD `d88a0d1f` (incluye `a5c5ad57` r2, `93f261c7` pin, `e3f8481d` entrega).
Clon limpio `git clone -s -n` -> `D:/Aegis_Scratch/protocol/rev0378r3`. Bancos de comportamiento en
`D:/Aegis_Scratch/protocol/w0378/{beh,p2a}`. Cinco puertas de `verification_cmd` en exit 0 alli.
Veredicto: `Area_comun/artifacts/Analista-TASK-0378-pin-y-r2-verdict.md`.

## Lo que aprendi, y me toca a MI

1. **Un negativo que se construye su propia victima no puede fallar.** El AC9 copiaba
   `.githooks/pre-commit` a un `mktemp`, le anadia una linea y comprobaba que `sha256sum` la
   rechazaba. Nunca le pregunta al guardia por el fichero REAL. Dado que el positivo pasa
   (pin == sha del gancho), que el negativo pase es una **tautologia**.
   **La forma correcta no es "perturbo algo y compruebo que salta": es "perturbo PRODUCCION y
   compruebo que salta el guardia de PRODUCCION".**
2. **El mutante que lo delata: M2.** Perturbar el gancho *y* desdentar el guardia (`|| true` en la
   linea del positivo). Si el paso sale exit 0 **y ademas imprime su mensaje de PASS**, el negativo
   es ciego. Complemento obligado: **M6**, borrar el bloque negativo entero -- si nada cambia, su
   aporte de deteccion es cero. Los dos son baratos. **Los corro por defecto contra cualquier
   negativo que se me entregue.**
3. **Extraer el paso de CI con parser YAML, no con regex,** y ejecutarlo con la invocacion REAL de
   GitHub Actions: `bash --noprofile --norc -eo pipefail step.sh`. Sin `-eo pipefail` el
   comportamiento del paso no es el que CI ve.
4. **Preguntar por el PERIMETRO del guardia, no solo por su logica.** El hallazgo mayor no estaba en
   el paso: estaba en que `.github/` no aparece ni en `GOVERNED` ni en `staged_product_paths` de
   `scripts/check_commit_trailers.py`. Medido: commitee el **borrado entero** del paso del pin
   (`chore: remove CI step`, sin `Task-Id`, sin claim, sin evento) y **aterrizo, rc=0**. Verificado
   por contenido: `git show HEAD:.github/workflows/validate.yml | grep -c PIN_MISMATCH_NEGATIVE`
   -> 0. Un gate nuevo hay que preguntarle **quien vigila el fichero donde vive el gate**.
5. **Separar generos de "literal".** El Arquitecto lo enruto a la familia 0397 (cardinales
   declarados). No es lo mismo: un cardinal declarado diverge **en silencio** -> verde falso; este
   pin se compara contra el digest **recomputado del artefacto real** -> diverge **a gritos** ->
   rojo. Su defecto real es el **radio de explosion**: paso **3 de 83**, si falla se saltan 80 y
   solo 2 llevan `if: always()` -> **78 saltados**, que casa exacto con la corrida 31913703515.
   Nombrar mal el genero enruta el arreglo al sitio equivocado.
6. **Mi harness me mintio una vez.** `vec()` hacia `git reset -q` al final, asi que el vector
   siguiente corrio con el indice VACIO y salio "BLOCKED" por `nothing to commit`, no por el gate.
   El camino feliz (AC5) parecio FAIL y no lo era. **Confirmar todo BLOCKED con el veredicto
   AISLADO del gancho (`check_commit_trailers.py --pre-commit` y el hook de commit-msg por exit
   code), no solo con el movimiento de HEAD.**

## Estado sustantivo que hay que recordar

- **r2 esta bien.** P-CAUSA cerrado: `claim_state` devuelve `owned`/`other`/`missing` y cada rama
  emite una frase **cierta** (antes colapsaban en un booleano y la frase era literalmente falsa).
  P-2A parcialmente cerrado: `.githooks/commit-msg` ya deriva `instance_root` desde `dirname $0` y
  **arranca** en 2.A; medido accept exit 0 / reject exit 1 en un 2.A sintetico.
- **AC8 PASS** por exit code y por mutacion (M1: gancho perturbado -> el paso muere).
- **AC9 FAIL** como instrumento; la propiedad que promete es cierta, pero la acredito yo, no la
  entrega.
- Residuales vivos: SLIP AC4 (con prefijo no vacio los suites solo prueban ACEPTAR --
  `test_precommit_hook.py:261-262`, `test_commit_msg_hook.py:99-102`); P-2A fuera de prefijo
  (`src/app.js` pasa los dos ganchos exit 0); P-IDENT y P-LEDGER-CLAIM en **TASK-0386**
  (verificado `proposed`, owner Codex, reviewer Analista -- mi condicion del 14-ago cumplida);
  `.githooks/commit-msg` **sin pin** (asimetria en la peor direccion: el autoritativo es el libre).
- Bucle: **iteracion 2 de 2**. La siguiente escala al operador humano. R1 = negativo que llame al
  MISMO instrumento que el positivo, aceptacion por mutacion. R2 = instrumento LOCAL que ate el
  gancho a su pin. R3 = decision del Arquitecto sobre `.github/` en el perimetro.

## Mecanica

Trailer que basta: `Task-Id: TASK-0378` + `Ops-Reason: adversarial-review-verdict`. El asunto no
debe empezar por `fix|revert|hotfix` o el gate exige `Fixes-Task`. `validate_collaboration_state.py`
tarda ~2 min: **background desde el principio**. Anticolision: la unica reclamacion activa era
`CLAIM-20260816-Codex-TASK-0337-r2` y su scope no toca `Area_comun/artifacts/` ni `mailbox/open/`.
El gancho avisa `PRUNE DUE` sin bloquear (ya senalado por el Operador, no es mio).
