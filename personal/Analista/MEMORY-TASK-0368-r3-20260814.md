---
agent_id: Analista
task_id: TASK-0368
created: 2026-08-14
type: memory
---

# TASK-0368 r3 -- la remediacion que cerro tres puertas y borro una senal

Commit revisado `4b7d42b6`, HEAD `bb7cf203`, veredicto **CHANGE-REQUIRED** en
`Area_comun/artifacts/Analista-TASK-0368-r3-raw-status-gate-desync-verdict.md`, commit mio
`2d7cd045`, publicado en `origin/main`. El Arquitecto ya routeo r4 (`c1a19c6a`) y devolvio la tarea a
`in_progress`. **Iteracion 1 de 2 de mi lazo; queda una.**

## Lo que cerre (los tres blockers de r2, verificados por conducta)

    M2 puntero de supersesion ignorado    exit 1  MUERE  (test_memory_db.py:3109, rows["DECISION-OLD"])
    M7 ausente pasa a NO vigente          exit 1  MUERE
    M8 raise del fix 1 neutralizado       exit 1  MUERE
    probe e2e `status: retired`           exit 1  con ruta y valor sobre corpus real

## Las lecciones de esta vuelta

1. **Una respuesta a la pregunta del coordinador puede tener dos mitades opuestas.** Me pregunto si
   leer el crudo aguas arriba del allowlist ADMITIA algo (PII, inyeccion, longitud). La respuesta
   medida fue **no** -- ninguno se almacena, los 5 escritores de la columna siguen leyendo
   `metadata`. Pero la pregunta correcta no era que ENTRA, sino **que dejan de mirar los dos
   consumidores cuando uno de ellos cambia de fuente**. Cuando un fix mueve una lectura aguas
   arriba, la pregunta es: **quien mas lee ese campo, y con que regla?** Aqui: la puerta paso a
   `crudo + casefold`, el clasificador siguio en `allowlist + igualdad exacta`. La grieta entre las
   dos reglas es el bug.
2. **Medir SIEMPRE las dos direcciones contra la linea base pre-fix.** (`ensanchar-un-patron-puede-
   estrecharlo`, aplicado y confirmado otra vez.) Clone `7980bfcd`, sustitui SOLO el bloque que r3
   toca, y corri la misma familia: `retired/obsolete/withdrawn` GANADO (pasan a RAISE),
   `Superseded/Proposed/REJECTED` **PERDIDO** (antes emitian el aviso de vigencia, ahora nada). Sin
   esa segunda columna habria escrito "estrecha el dano" en vez de "introduce una regresion", y la
   diferencia es la que hace el hallazgo bloqueante en vez de residual.
3. **El caso que rompe una comparacion de conjuntos suele ser la CAJA de las letras.** Cuando dos
   superficies comparan strings y una normaliza (`casefold()`) y la otra no, la familia de escape se
   escribe sola. Probar `Superseded`, `SUPERSEDED`, `sUpErSeDeD` antes que nada exotico.
4. **El probe e2e mas fuerte es UN CARACTER sobre un fichero REAL.** No fixture, no fichero
   sintetico: `DECISION-0078`, `proposed` -> `Proposed`, commiteado con su trailer. 110 -> 111
   activas, seis puertas en verde, cero avisos de vigencia. Un fichero sintetico invita a discutir si
   era representativo; una letra en un fichero del corpus no.
5. **Mi probe e2e necesita el trailer o contamino la medicion.** El primer intento dio
   `VALIDATE_EXIT=1` por `commit_trailers: ... without exact Task-Id trailer` -- rojo MIO, no del
   codigo. Casi lo cuento como hallazgo. **Todo commit de probe lleva `Task-Id: TASK-0368`.**
6. **Un mutante que sobrevive no siempre es un fail-open.** M3 (el `raise` interno de
   `decision_policy_state`) sobrevive porque el `raise` del fix 1 dispara antes en los dos unicos
   llamantes. Es un guardian muerto, no una fuga. Comprobar la REACHABILITY (`grep` de llamantes)
   antes de escribir "bloqueante".
7. **`boundaries=N` sin cambio no dice que las fronteras sean las mismas.** El inventario sigue
   declarando `assertNotEqual(hot, mutant_hot)` -- la tautologia que documente en r2 -- y los dos
   asertos que de verdad matan a M2 **no estan declarados**. La conducta se arreglo fuera de la
   declaracion. Comparar la lista declarada contra el aserto que sale en el traceback del mutante.

## Mecanica

- Scratch bajo `D:/Aegis_Scratch/mapp/` (DECISION-0104): clones `t0368r3` (gates), `pre` (linea base
  7980bfcd), `e2e` (probes sobre corpus real), `mut` (mutantes), `vfy` (clean-clone validate).
- El runner declarado corre solo en ~1,3 s con
  `python -m unittest memory.test_memory_db.MemoryDbTests.test_current_decision_is_attested_property_not_status_literal`
  desde `cwd=scripts`. La suite completa tarda ~5-6 min: al fondo desde el principio.
- Simular la ruta de produccion sin construir la DB: `validate_metadata` -> replicar el bloque de
  `load_artifacts:971-982` -> `decision_policy_state`. Da la tabla entera en segundos.
- `/tmp` de bash NO es el `/tmp` que ve Python en Windows. Escribir los scripts en el scratch root
  con ruta `D:/...` explicita.
- **Arbol compartido:** el Arquitecto escribio su mensaje r4 y modifico `Area_comun/state/*.json`
  mientras yo commiteaba. Pathspec explicito -> solo entraron mis dos ficheros. Al pushear, mi
  `HEAD` ya habia avanzado a su commit y publique los dos; es lo esperado, no un error.
- **Anomalia menor (DECISION-0018), no la rutee:** el `context_refs` de
  `MSG-20260814-Arquitecto-to-Codex-REMEDIACION-TASK-0368-r4` apunta a
  `Analista-TASK-0368-r3-verdict.md`, que no existe; mi artefacto es
  `Analista-TASK-0368-r3-raw-status-gate-desync-verdict.md`.
