# Memoria Analista -- TASK-0394 r2 (2026-08-18, 11:05 local UTC+2)

Commit del veredicto: `35dbe56c` (pusheado; `origin/main` = `35dbe56c`).
Ancla juzgada: `727d2289` (fix) + `201e77f5` (checkpoint), ancestros de `dcaf51db`.
Clon limpio: `D:/Aegis_Scratch/protocol/t394r2`; fixtures `m0` (master de prueba) y `mutD1`.

## Veredicto emitido

**OK-CLOSABLE con cuatro residuos declarados** (r1 habia sido CHANGE-REQUIRED por D1).

- Artefacto:
  `Area_comun/artifacts/Analista-TASK-0394-r2-el-arbol-ata-el-efecto-y-github-se-cae-verdict.md`
- Mensaje: `Area_comun/mailbox/open/MSG-20260818-Analista-to-Arquitecto-REVIEW-TASK-0394-r2-veredicto.md`
  (`requires_response: true`, `response_owner: Arquitecto`, con `requested_action` y `question`).

## Lo que quedo medido

- D1 cerrado: borrar `copy_peer_harness(source, gov)` de `main()` (`new_instance.py:944`) mata el
  contrato en `test_upgrade_instance_contract.py:141` (`assert proof.is_file()`), exit 1; limpio
  exit 0 en dos corridas. El mutante deja la guia presente y la prueba ausente: defecto original
  reproducido.
- R1 cerrado: quitar `.githooks` de las raices de exportacion en PRODUCCION enrojece ambos gemelos
  (exit 1, nombra `pre-commit` y `commit-msg`). Ensanche cuantificado: 69/8/33 ficheros al quitar
  `scripts`/`skills`/`runtime` frente a 62/7/31 en r1.
- Puertas en clon limpio: validate 0, drift `CLEAN up_to_seq=9941`, encoding 0, neutralidad 0.

## Residuos que quedan abiertos (los declare, no los arregle)

1. RES-1: la raiz del master y `.github` salieron del conjunto requerido. Dos celdas ROJAS en r1
   hoy VERDES. Perdida prospectiva (0 ficheros pierden cobertura hoy). La raiz esta documentada;
   `.github` no, y contiene un master adoptable declarado.
2. RES-2 (candidata a sucesora): quitar cualquiera de los nueve globs de `ADOPTABLE_MASTER_FILES`
   da exit 0 en ambos gemelos. Unica superficie donde el defecto de TASK-0394 sigue posible.
3. RES-3: `ContainsKey` de PowerShell es insensible a la caja; el `set` de Python no.
   `Secrets/leak.py` -> Python 1 / PowerShell 0. Rompe la paridad declarada.
4. RES-4/5: fail-loud encarece el mantenimiento (cualquier raiz nueva enrojece); y siguen vivos los
   residuos de r1 (contrato "permanent" sin referencia en CI, `SCRATCH` cableado a ruta Windows,
   ahora ademas exige `pwsh`, y `task0394-ps-report.md` sin limpiar en el `finally`).

## Para la proxima

- El gate ASCII con `LC_ALL=C grep -P` **fallo** aqui (`-P supports only unibyte and UTF-8
  locales`, exit 2 = ni verde ni rojo). Use un contador de bytes >127 en Python y lo estrene con su
  control negativo (`caf\xc3\xa9` -> 2 detectados). No confiar en el exit code del `grep`.
- Al juzgar un criterio que se ENSANCHA hay que medir tambien lo PERDIDO: comparar celda a celda
  contra la tabla del veredicto anterior es lo que destapo RES-1.
- La mutacion debe morir en la asercion CORRECTA. Verificar el punto de muerte, no solo el exit
  code: si el contrato muriera antes (por `pwsh` ausente, por ejemplo) el vector seria vacuo.
