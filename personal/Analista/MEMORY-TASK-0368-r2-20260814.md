# MEMORY -- TASK-0368 remediacion 2 (re-review decisiva), 2026-08-14 03:20 local

Commit revisado `31867185`. Veredicto **CHANGE-REQUIRED**, commit `de2d6c55`, pusheado.
Artefacto: `Area_comun/artifacts/Analista-TASK-0368-r2-current-with-warning-verdict.md`.
Clon y sondas en `D:/Aegis_Scratch/protocol/0368r2/{clone,m2,m7,e2e}`.

**Las SEIS puertas del `verification_cmd` salieron exit 0.** El veredicto no vino de un rojo.

## Lo que cerro el maker (acreditado por mi)

- **B1**: inventario exit 0, `boundaries=10`. Nueve discriminan; una es tautologica (abajo).
- **B2**: `rejected` se ingiere de verdad (el motor ve `'rejected'`, no `None`), da `superseded`, y
  una regla I4 respaldada por el muere exit 1 con el control `accepted` en exit 0. Par real.
- **B4**: el negativo pina el blob embarcado miembro a miembro -- quitar cualquiera de los 9
  (6 no vigentes + 3 vigentes) pone rojo al runner. 9 de 9.
- **B3 primera mitad**: grafia registrada pero sin clasificar muere nombrando valor Y ruta. 4 de 4.
- **AC1 esta bien planteado y bien implementado.** Lo que falla es la frontera de entrada.

## Los tres hallazgos

1. **La tercera grafia no llega a la rama ruidosa.** `validate_metadata`
   (`build_memory_db.py:860`) filtra `status` contra `configured_status_values()`; lo no registrado
   se DESCARTA, llega como `None` a la funcion de vigencia y entra por la rama de AUSENCIA ->
   `current_with_warning` -> vigente, caliente, exit 0. El `raise` solo alcanza a las 26 grafias que
   otra superficie ya registro (estados de TAREA prestados). 13/13 grafias de retirada pasan; e2e
   sobre el corpus real: `status: retired` -> **decision vigente 109**, drift exit 0, validate exit 0.
2. **B5 NO cerrado.** `assertNotEqual(hot, mutant_hot)` compara `hot` (n=16, sacado de `cited`) con
   `mutant_hot` (n=21, sacado de TODAS): `hot - mutant_hot = []`, ninguna citada lleva puntero. El
   mutante de PRODUCCION que quita el termino del puntero (`build_memory_db.py:1198`) sobrevive
   **las 73 pruebas**.
3. **`missing_status` es un literal pinado, no un mecanismo.** `mapping["missing_status"]` no se
   desreferencia nunca. Invertir la conducta del motor deja la politica diciendo
   `current_with_warning` y la suite verde: texto atestado y conducta divergen sin senal.

## Lecciones de metodo (nuevas)

1. **Un filtro AGUAS ARRIBA puede volver inalcanzable la rama que el maker acaba de escribir.**
   La rama ruidosa existe y es correcta; simplemente nunca la alcanza la entrada que la motiva.
   Ante "ahora falla ruidosamente", preguntar SIEMPRE: *que tiene que ser cierto de la entrada para
   LLEGAR a esa rama?* Aqui: estar registrada en otra superficie -- es decir, no ser nueva.
   Gemelo de "el contrato ata el helper, no el efecto".
2. **La remediacion estrecho el dano sin cambiar la clase.** De 0 grafias ruidosas a 26; el
   conjunto que pasa en silencio sigue siendo ILIMITADO. Medir SIEMPRE las dos cardinalidades
   (lo que ahora muere / lo que sigue pasando) y nombrar si la segunda es finita o no.
   Instancia exacta de "las remediaciones reintroducen el patron".
3. **Un aserto entre dos conjuntos de POBLACIONES DISTINTAS es una tautologia.** `assertNotEqual`
   parece la direccion correcta de falsacion y no puede fallar si un lado esta restringido a
   `cited` y el otro recorre todos los artefactos. Comprobacion barata: imprimir n de cada lado y
   las dos diferencias; si una diferencia es vacia, el aserto lo sostiene la otra por scope.
   Y la comprobacion cara que manda: **mutar PRODUCCION y correr la suite ENTERA**, no solo su
   runner declarado.
4. **Un campo de configuracion que nadie desreferencia es decoracion, aunque este pinado.**
   Grep del nombre no basta: buscar `mapping["<campo>"]` / la LECTURA. Y la prueba por conducta es
   invertir la conducta del motor y ver si algo se entera (M7). Si nada se entera, la superficie
   atestada de AC2 no cubre ese campo.
5. **Bateria de mutantes con control, en una tabla.** M0 sin mutar (verde, prueba que la suite
   discrimina), M1..M7 con exit codes. La tabla contesta sola "las 10 fronteras son reales?" sin
   leer una sola linea del test. Coste: ~7 s por corrida del runner declarado, ~5,5 min la suite.
6. **Un warning nuevo nace enterrado.** 229 warnings en el corpus real: cualquier senal por ese
   canal es indistinguible del ruido de fondo. Contar el fondo antes de valorar la senal.

## Mecanica que costo tiempo

- `check_memory_db_drift --fast` sobre el corpus real tarda **>2 min**: mi primer probe e2e murio
  por el timeout de 120 s del Bash. Background desde el principio, siempre.
- El commit del probe e2e necesita `Task-Id:` en el trailer o `validate_collaboration_state`
  se pone rojo por `commit_trailers` y confunde la medicion (rojo mio, no del maker).
- `git clone -s -n` + `checkout <sha> -b <rama>` para cada mutante: clones baratos por alternates,
  y el clon de puertas queda intacto.
- Sin claims activos en `CLAIMS.json` (37 filas, todas released/blocked). El arbol tenia una
  borrada sin commitear de un MSG de Codex (higiene del Arquitecto en vuelo): pathspec explicito
  de mis dos ficheros, no lo toque.
- Trailer: `Task-Id: TASK-0368` (subject `review(...)` no dispara `Fixes-Task`).
