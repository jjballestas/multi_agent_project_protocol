# MEMORY -- TASK-0337 r2, veredicto CHANGE-REQUIRED (2026-08-16, 06:05 local)

Commit del veredicto: `92a8f180` (pusheado a `origin/main`).
Artefacto: `Area_comun/artifacts/Analista-TASK-0337-r2-guard-residuo-scope-aware-verdict.md`
Mensaje: `Area_comun/mailbox/open/MSG-20260816-Analista-to-Arquitecto-REVIEW-TASK-0337-r2-CHANGE-REQUIRED.md`
Ancla revisada: HEAD `623fb8b4`, entrega `08acce5d`, fix r2 `d4c2e8c3`, control `67e58387`.
Clones limpios: `D:/Aegis_Scratch/protocol/rev0337` y `.../ctrl0337`. Banco propio: `rev0337/adv0337.py`.

## Lo que hay que recordar del METODO, no del caso

1. **Una rama de fallo-cerrado puede contener el defecto entero.** El arreglo de 0337 hace
   scope-aware el guardia SOLO cuando el scope del mensaje resuelve. La rama `$null` conserva la
   regla vieja. El AC estaba escrito sin condicion ("su siguiente mensaje"), asi que el arreglo
   parcial pasa cualquier prueba que solo use el camino feliz. **Ante un arreglo con dos ramas,
   ejercitar SIEMPRE la rama degradada con el mismo par de vectores.**

2. **Peso de campo DERIVADO, no ejemplificado.** No basta con decir "puede pasar con mensajes sin
   task_id". Se cuenta: 292/458 tareas sin `scope_routes` parseable, 10/38 mensajes abiertos sin
   `task_id` numerado. La cifra convierte una esquina en la mayoria del corpus y es lo que hace
   bloqueante el hallazgo.

3. **Un negativo puede matar dos mutantes y dejar vivo el tercero, que es el suyo.** El contrato
   `retry-residue-scope-pair` mata el mutante de prefijo y el de codigo muerto, pero SOBREVIVE al
   borrado de la exencion que le da nombre, porque en su fixture la prueba de interseccion ya
   produce el verde. **Regla: mutar la clausula que el negativo NOMBRA, no solo las que el runner
   lista.** Y probar despues que la clausula no es codigo muerto (con un scope que la haga
   load-bearing, produccion y mutante deben divergir).

4. **Un sub-assert puede ser vacuo aunque el caso entero discrimine.** El bloque del prefijo
   (lineas 1379-1389 del runner) ejercita el mutante contra un vector donde produccion y mutante
   hacen LO MISMO (ambos arrancan), y "mata" al mutante exigiendole lo contrario. Quien mata de
   verdad al mutante de prefijo es el vector anidado/ajeno del bucle de arriba. Verificado mutando
   la PRODUCCION copiada, no la cadena `.replace()` del runner.

5. **`git -C <dir> rev-parse --show-prefix` se evalua respecto a `<dir>`, no al CWD del proceso.**
   Y `git status --porcelain` emite rutas relativas a la raiz del REPOSITORIO desde cualquier CWD.
   Los dos lados casan. Dato duro para futuras revisiones de layout anidado.

6. **Rojo del control historico = rojo no atribuible.** `test_exec_lease_harness.py` sale exit 1
   (3/31) en el commit revisado; el MISMO exit 1 con los MISMOS tres fallos en `67e58387`, anterior
   al fix. Sin ese control lo habria colgado de la entrega. Corolario: esta tarea **no se cierra por
   exit code de su propia `verification_cmd`**.

7. **La `verification_cmd` declara el contrato pero NO lo ejecuta.** `check_falsification_contracts.py`
   verifica la DECLARACION; el contrato 0337 solo corre invocando
   `run_mailbox_retry_cases.run_residue_scope_pair_case()` a mano. Lo corri yo.

8. **Antes de colgar un residuo de otra tarea, abrir su `out_of_scope`.** TASK-0405 delega el
   interbloqueo propio a "TASK-0406", pero el cuerpo de TASK-0337 registra que TASK-0406 se evaluo
   y se DESCARTO por duplicar AC6/AC7. El defecto es de 0337 por decision escrita.

## Banco de pruebas reutilizable

`D:/Aegis_Scratch/protocol/rev0337/adv0337.py` lanza el cron REAL sobre repos git de laboratorio y
parametriza: `nested` (gobierno en `Aegis/` o en la raiz), `cwd_at_root` (lanzador en la raiz del
repo o en la instancia), `dirty_rel`, `msg_task_id`, `scope_routes`. Discriminante de conducta:
`EXEC_START` en el log + `reason=` + `intersections_json=`. Diferencia deliberada con el fixture
entregado: la memoria propia esta VERSIONADA y modificada (` M`), no solo sin seguir (`??`).

## Estado al cerrar

El Operador decidio en `4fe87dc2` que 0337 viaja a NOVA tal cual y el fix de H-1 va en paralelo.
Mi bucle declarado: iteracion 1 de 2; rejuicio en clon limpio antes del commit de cierre; a la
tercera, escalada al operador humano.
