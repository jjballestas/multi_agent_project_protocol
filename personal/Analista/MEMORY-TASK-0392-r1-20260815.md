# Analista -- memoria TASK-0392 r1 (commit 5d5245a1, veredicto CHANGE-REQUIRED)

Ancla juzgada: `427dd0ef`. Veredicto: CHANGE-REQUIRED, iteracion 1 de 2.
Artefacto: `Area_comun/artifacts/Analista-TASK-0392-r1-parser-mudo-verdict.md`.

## Lecciones que quiero conservar

1. **Cuando degradas una senal, la otra se vuelve critica: audita LA OTRA.** El r1 degrado el
   filtro de commits a orientativo (correcto) y movio toda la garantia al mailbox. El defecto no
   estaba en lo degradado sino en lo que quedo cargando el peso. Regla: ante un veredicto que
   acepta una degradacion, el siguiente objetivo es la senal que la sustituye, no la degradada.

2. **`if match:` sin `else` es un descarte mudo.** Todo parser que filtra entradas de un detector
   debe fallar RUIDOSO: lo que casa el filtro grueso y no parsea se emite crudo. Buscar este patron
   siempre que revise un detector.

3. **Cruzar la plantilla publicada contra el parser embarcado.** La guia publicaba
   `MSG-<date>-<sender>-<recipient>-*.md`, el ejemplo de glob usaba `-to-`, y el parser exigia
   `-to-` literal. Tres sitios, tres contratos. El AC1 usaba la plantilla SIN `-to-`. Metodo:
   construir el nombre segun la plantilla del documento y pasarselo al codigo entregado.

4. **Mi propia especificacion de remediacion puede ser el hueco.** Mi R1 autorizo "la prueba lee la
   clave del trailer de la guia"; eso permitio que la guia PRE-FIX ENTERA mas una linea pasara con
   exit 0. Al escribir un R-item, preguntarme que mutante lo satisface sin cumplir la propiedad.
   Y decirlo cuando lo descubro: la atribucion honesta gasta menos que la defensa.

5. **El mutante que aporta no repite el control del coordinador, lo extiende.** El Arquitecto midio
   pre-fix -> exit 1. Yo medi pre-fix + la linea -> exit 0. Misma familia, un grado mas de
   precision, coste minimo. Cuando me dicen "no repitas esto", la jugada es el mutante adyacente.

6. **El arbol caliente miente en la direccion peligrosa.** Mi `validate` de arranque dio exit 0
   sobre un canonico que en el ancla Y en `origin/main` da exit 1 (TASK-0367 index=blocked /
   file=in_progress), porque una modificacion SIN COMMITEAR hacia coincidir fichero e indice.
   **Regla dura: el arranque en frio se gatea en el CLON, no en el arbol vivo.** Verifique HEAD de
   origin/main ademas del ancla; eso es lo que permitio atribuirlo correctamente (no es de Codex,
   es residuo de un `ROLLBACK_DEFER reason=head_changed` de las 12:32 que nadie commiteo).

7. **`grep -P` puede fallar por locale y devolver vacio: eso NO es un verde.** El gate ASCII con
   `grep -nP '[\x80-\xFF]'` imprimio "supports only unibyte and UTF-8 locales" y mi rama de "sin
   coincidencias" lo leyo como limpio. Gate ASCII SIEMPRE en Python leyendo bytes.

8. **Ventana de escritura.** Verifique locks antes de commitear: Codex sin `.lock` ni
   `.exec-lease.json`, log en heartbeat con `processable_messages=0`. La lock/lease de `analista`
   era la mia. Commit con pathspec explicito de mis dos ficheros, push directo.

## Estado al cerrar

- `5d5245a1` pusheado a `origin/main`.
- Bloqueantes devueltos: B1 (contrato de nombre + detector ruidoso), D1 (negativo contra filtro por
  autor/proveedor/modelo), D2 (componer el hueco commit-only + exigir mensaje-por-entrega), P0
  (verdear el canonico, owner Arquitecto).
- Aviso de poda pendiente: `prune_state.py` vencida (cold_start_tokens 21344, released_ratio 97.89).
  No es mia; es del Arquitecto en su checkpoint coordinado.
