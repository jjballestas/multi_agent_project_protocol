# MEMORY -- TASK-0337 r3 (H-1 / H-2), 2026-08-16 07:45 local

**Commit del veredicto:** `72d775be` (pusheado a `origin/main` sobre `e721c6a8`).
**Ancla revisada:** HEAD `96e03f0b`; fix H-1 `f2de3ad7`; control `f2de3ad7^`.
**Veredicto: CHANGE-REQUIRED.** Iteracion 2 de 2 -- declarado que no abro la 3 sin el operador.
Banco: `D:/Aegis_Scratch/protocol/rev0337h1/{adv0337h1.py,mut0337h1.py}`.

## La leccion cara: me falto el control de VACUIDAD y atribui mal

En r2 medi que un mensaje con scope no resoluble + residuo propio difiere por
`worktree_residue_live`, y conclui que el guardia de residuo mantenia vivo el interbloqueo para el
64 % del corpus. La medicion era correcta; **la atribucion no**. Nunca corri el mismo vector **sin
residuo alguno**: tambien difiere, por `message_scope_ambiguous` (`Acquire-ExecReservation`,
`peer_mailbox_cron.ps1:1206`, incondicional cuando `Get-MessageWorkDescriptor` da `$null`). El exec
**nunca fue alcanzable** en esa familia, luego el criterio de aceptacion que escribi ("el exec
ARRANCA") era **insatisfacible tocando el guardia**, y mande al maker a arreglar la puerta
equivocada.

**Regla que me llevo:** antes de atribuir un bloqueo a un guardia, **quita la causa que le atribuyes
y vuelve a medir**. Si el bloqueo sigue, el guardia no era la restriccion que ata. Es el gemelo del
"verde que el codigo viejo tambien produce", en rojo: un ROJO que el arbol tambien produce sin tu
sospechoso no acusa a nadie.

## Lo segundo que casi se me escapa: el efecto en el arbol REAL, no en el fixture

El fix hacia lo pedido en el fixture. Censo del arbol vivo (`git status --porcelain -z` completo, no
un ejemplo): **1380 rutas sucias, el 100 % bajo `personal/`** -- Codex 1318, Arquitecto 52, operador
9, Analista 1. Para los dos peones hay SIEMPRE residuo propio Y ajeno co-presente, y en esa
composicion la conducta y la causa emitida son **identicas antes y despues**. El vector que importa
no es el del fixture: es **la composicion real del arbol**. Anadirlo como W5 fue lo que convirtio
"mejora parcial" en "efecto nulo".

## Lo tercero, y el unico hallazgo no opinable: el gate de contratos

`f2de3ad7` invirtio `test_residue_excludes_foreign_personal_and_caps_diagnostics` (y los renombrados
de `test_preexec_defer_budget_kills_shared_counter_mutant`) sin realinear
`NEG-HARNESS-PREEXEC-DEFER-STARVATION`, que sigue declarando las aserciones borradas ->
`check_falsification_contracts` pasa de **0** (`f2de3ad7^`) a **1** (`192d8338`, `96e03f0b`), y esta
cableado en CI (`validate.yml:305`). **Correr las CINCO puertas siempre, tambien las que salieron
verdes la vez anterior**: en r2 esta salio 0 y por eso casi la doy por descontada. Un A/B por commit
del propio gate (`git checkout <commit> -- scripts/ examples/`) cuesta 2 minutos y convierte
"esta rojo" en "lo rompio esta entrega".

Ademas el fondo: el veto nuevo cae sobre `personal/<otro>/**`, que bajo DECISION-0016 el bloqueado
tiene **prohibido** limpiar. Fallar cerrado sobre una condicion que la parte bloqueada no puede
remediar es un cambio de frontera, no un renombrado de test.

## H-2: como quedo el negativo

Mutando PRODUCCION y corriendo el contrato entero: M1 (borrar la exencion no resoluble) **exit 1,
muere** por la senal correcta (la causa vuelve a `worktree_residue_live` -- lo verifique leyendo el
log del mutante, no solo el exit code); M2 (rama -> `$false`, "eximir a todo") **exit 1**; M3 (la
exencion de la rama resoluble, mi mutante de r2) exit 1 pero **por TEXTO** -- el propio patron de
sustitucion del contrato desaparece; ningun vector la pisa por conducta porque el residuo propio del
fixture nunca intersecta el scope; M4 (exencion propia en la rama de renombrados, `:944`) **exit 0,
SUPERVIVIENTE**.

**Regla:** cuando un arreglo toca **dos ramas gemelas** (candidate y source/rename), mutar **las
dos**. La segunda casi nunca tiene vector.

## Mecanica

- El maker declaro `Area_comun/protocol/FALSIFICATION_CONTRACTS.json` en el `scope` de su claim y no
  toco el fichero: **el scope declarado predijo el fallo**. Leer el scope del claim del maker como
  lista de sitios donde mirar.
- `scan_encoding.py` puede reventar con `PermissionError` en arbol compartido si un peer escribe a la
  vez: **reintentar**, no interpretarlo como rojo.
- El Operador decidio (`e721c6a8`) mientras yo commiteaba, citando mi mensaje: revert de `f2de3ad7`,
  0337 viaja con AC7+AC10, la frontera DECISION-0016 al ciclo post-corte. **Hacer `git fetch` justo
  antes de commitear** y ajustar `requested_action`/`question` a lo que sigue vivo, en vez de
  reabrir lo ya decidido.
- Arbol compartido con el Arquitecto escribiendo `Area_comun/state/*` y el fichero de TASK-0337:
  pathspec explicito de mis dos ficheros, nada mas.
