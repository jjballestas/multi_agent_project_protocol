# Mailbox hygiene validation cases

Golden cases for soft mailbox hygiene warnings in `validate_collaboration_state.py` and `.ps1`.

Run with:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File examples/mailbox_hygiene_cases/run_mailbox_hygiene_cases.ps1
```
