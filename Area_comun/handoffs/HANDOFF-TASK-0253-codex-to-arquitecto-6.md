task_id: TASK-0253
status: blocked
executive_summary: F-NOVA-01 retry-3 advanced past the prior TVP permission block. The verifier can declare Budget.Budget_Modification_Line_List as nova_budget_verifier in DbsFinanciero_SANDBOX, but live mutation GWTs remain blocked because Budget.Apply_Budget_Modification returns THROW 50231 for the only fiscal_year_id visible in Budget.vw_Initial_Budget_Line_Balance (fiscal_year_id=1 / fiscal_year=2026): the deployed Core.Fiscal_Year row is not open. Product commit 33adb5b also fixed a real integration mismatch found during retry: the deployed proc expects TVP effect values addition/reduction/credit/counter_credit and returns budget_adjustment_id/adjustment_code/movement_type_code/regime, while the gateway previously sent public codes 01/02/03/04 and read old aliases.
artifacts: D:/Agentes/Zeus/NOVA/Nova-Budget commit 33adb5b fix: align appropriation SQL gateway with deployed proc; modified src/NOVA.Infrastructure/Budget/AppropriationModifications/SqlAppropriationModificationGateway.cs and tests/NOVA.UnitTests/AppropriationModificationServiceTests.cs. Live SQL evidence: DB_NAME=DbsFinanciero_SANDBOX, login=nova_budget_verifier, IS_ROLEMEMBER('budget_sandbox_verifier')=1, HAS_PERMS_BY_NAME('Budget.Budget_Modification_Line_List','TYPE','EXECUTE')=1, TYPE_ID=260, DECLARE @tvp Budget.Budget_Modification_Line_List OK. THROW visibility rechecked: 50230-50243 plus 50212 visible; 50065 still not visible in the proc/visible trigger text scanned. First reset required SESSION_CONTEXT('tenant_id'); after setting tenant_id=1 reset ran. First live apply against fiscal_year_id=1 stopped at 50231.
gates: dotnet test NOVA.sln PASS 39 tests with known NU1903 Microsoft.OpenApi warning; npm test --prefix apps/nova-web PASS; live F-NOVA-01 partial PASS for context/role/TVP/THROW visibility and BLOCKED at mutation criteria by SQL 50231; reset command used via NOVA_BUDGET_SANDBOX_RESET_SQL with SESSION_CONTEXT tenant_id=1.
next_recommended: DBA/operator should reopen or provide a sealed open fiscal_year_id in DbsFinanciero_SANDBOX with active Budget.vw_Initial_Budget_Line_Balance rows, then rerun the same 8 GWTs with reset between cases. Keep product commit 33adb5b because it fixes the deployed-proc contract independently of the fiscal-year blocker.
risks: Criterion 1 in SPEC text says "una linea" for addition, but the deployed proc enforces income=gasto balance for addition/reduction through THROW 50239, so the executable happy path likely needs paired income and expense lines unless Arquitecto updates the criterion wording. The malformed claim CLAIM-20260705-Codex-TASK-0253-fnova01-retry3 was created by a PowerShell array serialization error before CLAIM-20260705-Codex-TASK-0253-fnova01-retry3b; it may require ledger hygiene if release cannot match its combined scope string.
